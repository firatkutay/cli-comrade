// Package executor will run generated commands on the host platform (sh -c
// on Unix, powershell -Command on Windows) and stream their output back to
// the behavior-mode loop (introduced in FAZ 6). It is an empty placeholder
// in FAZ 0.
package executor
