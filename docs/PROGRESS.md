# PROGRESS
module_path: github.com/firatkutay/cli-comrade
current_phase: 11
status: done

## Tamamlanan Fazlar
- [x] FAZ 0 — commit: 33c92ec
- [x] FAZ 1 — commit: 18e6baa
- [x] FAZ 2 — commit: 105a726
- [x] FAZ 3 — commit: b50852b
- [x] FAZ 4 — commit: c31f71a
- [x] FAZ 5 — commit: 6f4b1c3
- [x] FAZ 6 — commit: dd15f48
- [x] FAZ 7 — commit: f6b89ba
- [x] FAZ 8 — commit: a90d74e
- [x] FAZ 9 — commit: 5b8fca4
- [x] FAZ 10 — commit: 04d52c8
- [x] FAZ 11 — commit: afc31ae

## Notlar / Ertelenen İşler

### Tamamlandı
- Stream goroutine sızıntısı kapatıldı (FAZ 6 sertleştirme kalemi): anthropic/google/ollama/openai_compat connector'larının kanal gönderimleri artık `sendChunk` (internal/llm/client.go) ile `ctx.Done()` select'i üzerinden korumalı; `Client.Stream`'in `releaseOnClose` forward goroutine'i de aynı deseni kullanıyor. Tüketici kanalı erken terk edip (Ctrl-C) ctx iptal edildiğinde üretici goroutine artık sonsuza kadar bloklanmıyor. Regresyon testleri (`-race` altında, `runtime.NumGoroutine()` baseline'a döndüğünü doğrular): `TestAnthropicStreamGoroutineExitsWhenContextCancelledWithoutDraining`, `TestGoogleStream...`, `TestOllamaStream...`, `TestOpenAICompatStream...`, `TestClientStreamGoroutineExitsWhenContextCancelledWithoutDraining` (internal/llm). Fix olmadan tüm 5 test 3sn timeout'ta fail ediyor — doğrulandı.
- Idle-timeout değerlendirildi ve uygulandı: yeni `llm.idle_timeout_seconds` config anahtarı (varsayılan `0` = kapalı, mevcut davranışla birebir aynı). Tek chunk arası boşluğu `Client.Stream`'in `releaseOnClose`'unda merkezi bir timer ile sınırlıyor — connector'ların kendi okuma döngülerine dokunmadan (temiz, katmanlı ek). `time.Timer` sıfırlama Go 1.25 (bu modülün sürümü) hedefiyle bilinçli olarak Stop()+drain YAPMIYOR: Go 1.23'ten beri Timer kanalı senkron (buffersız) ve çıplak `Reset` bayat değer sızdırmaz (`go doc time.Timer.Reset`); pre-1.23 Stop+drain deyimi burada TERSİNE tehlikeli olurdu — select aynı anda hem chunk hem timer ateşini görüp chunk dalını seçerse, Stop() false döner ama `<-idleTimer.C` üzerinde asla gelmeyecek bir değeri beklerdi (tam da bu değişikliğin kapatmaya çalıştığı sınıftan bir goroutine sızıntısı). Bu, review turunda gerçek bir bulgu olarak yakalandı ve düzeltildi; ilgili kod satırında kalıcı açıklayıcı yorum var (client.go). Yeni sentinel: `ErrIdleTimeout`. Tablo tabanlı testler: `TestIdleTimeoutDurationDisabledForNonPositive`, `TestClientStreamIdleTimeoutAbortsWhenGapBetweenChunksExceeded`, `TestClientStreamIdleTimeoutDisabledByDefaultAllowsSlowGaps` (internal/llm); config şema/validate testleri (internal/config). Belgelendi: docs/CONFIGURATION.md (TR+EN). Kullanıcı görünür yeni metin yok (internal/llm paketinin mevcut i18n istisnası kapsamında — Stream henüz hiçbir CLI/engine akışına bağlanmadığından hata TUI'ye yansımıyor).
- FAZ 10 kalemi (install.sh/install.ps1 arşiv adı drift testi): tamamlandı — `internal/cli/release_names_test.go` mevcut.
- FAZ 10 kalemi (wget fallback + önkoşul kontrolü): tamamlandı — `scripts/install.sh`'ta `require_downloader` (curl/wget sırayla arar, ikisi de yoksa açıklayıcı hata) + `fetch_url`/`fetch_url_to_file` (tek kod yolu, hangisi bulunduysa onu kullanır).
- Bulgu (bağımsız review) çözüldü: ask-modu onay satırı artık `general.language`'ı takip ediyor. `internal/tui/confirm.go`, `PromptChoice`/legend/edit-başlığı metnini `internal/i18n.Translator` üzerinden çözüyor (yeni `MsgConfirmLegend`/`MsgConfirmEditHeader` katalog anahtarları, TR+EN); `mapKey` artık `lang` parametresi alıyor ve kabul edilen tuş kümesi dile göre **kesinlikle ayrık** — birleşim (union) değil: TR `e`=evet vs EN `e`=edit, TR `a`=açıkla vs EN `a`=tümü çakışmasını önlemek için. `Confirm`/`newConfirmModel` imzalarına `tr i18n.Translator` eklendi; `internal/cli`'de `tuiPromptUI` (do.go/fix.go/chat.go, promptui.go) aynı `newTranslator(cfg)` zincirini enjekte ediyor — ayrı bir dil çözümü yok. `internal/cli/catalog_coverage_test.go`'daki i18n linter'ı `WriteString(...)` çağrılarını da taramaya genişletildi (`findRawWriteStringLiterals`) — önceden confirm.go'nun `strings.Builder.WriteString` kullanımı bu linter için kör noktaydı (yalnızca `fmt.Print*/Fprint*` görüyordu); genişletilmiş tarayıcı, eski (sabit-Türkçe) confirm.go içeriğine karşı çalıştırılıp 2 sabit literal yakaladığı, düzeltilmiş dosyaya karşı 0 bulduğu doğrulandı. Testler (internal/tui/confirm_test.go): `TestMapKeyTRLetters`, `TestMapKeyENLetters` (regresyon bekçileri: EN `e`→Edit [Yes DEĞİL], EN `a`→All [Explain DEĞİL]), `TestConfirmModelUpdateENPressEEntersEditNotYes`, `TestConfirmModelUpdateENPressAQuitsWithAllNotExplain`, `TestConfirmModelUpdateENTRLettersDoNothing`, `TestConfirmModelViewShowsTRLegend`/`ENLegend`, `TestConfirmModelViewShowsEditPromptWhileEditingTR`/`EN`, `TestConfirmRunsHeadlessProgramENPressAIsAllNotExplain` (tam bubbletea Program üzerinden uçtan uca). Gate: `go vet ./...`, `golangci-lint run`, `go test ./... -count=1`, `go test -race ./internal/tui/... ./internal/engine/...` temiz.
- Windows'ta `general.language = "auto"` artık gerçekten otomatik: `internal/i18n.ResolveLanguage`'ın zincirine, İngilizce'ye düşmeden hemen önce yeni bir adım eklendi — `systemLocale func() string` (enjekte edilebilir, `getenv` ile aynı desende; üretimde `i18n.SystemLocale`), yalnızca `COMRADE_LANG`/`LANG`/`LC_ALL` hepsi boşken tüketiliyor. Yeni sıra: config açık `tr`/`en` > `COMRADE_LANG` > `LANG` > `LC_ALL` > **Windows sistem yereli** > İngilizce. Windows'a özgü ham syscall (`kernel32.dll`'den `GetUserDefaultLocaleName`, stdlib `syscall.NewLazyDLL`/`LazyProc` ile — yeni bağımlılık yok) yalnızca `internal/i18n/locale_windows.go`'da (`//go:build windows`) izole edildi; `internal/i18n/locale_other.go` (`//go:build !windows`) her zaman `""` döner — bu, CLAUDE.md'nin runtime.GOOS tercihine karşı gerekçeli, belgelenmiş bir build-tag istisnası: kod tabanındaki iki build-tag izolasyonundan biri; diğeri internal/executor'ın süreç-grubu syscall çifti (executor_unix.go/executor_windows.go) — ikisi de aynı gerekçeyle var (ham bir platform syscall'ı diğer GOOS'ta derlenemiyor). Üç OS'ta test edilebilirlik yine de korunuyor: karar mantığının tamamı saf ve platformdan bağımsız kalıyor, `ResolveLanguage`'ın enjekte edilebilir `systemLocale` parametresi sayesinde herhangi bir host GOOS'tan test edilebiliyor. `ResolveLanguage`'ın tüm çağıranları güncellendi: `internal/cli` (help.go, runtime.go — ikisi de `i18n.SystemLocale` geçiyor), `internal/engine` (Diagnoser/Explainer/Planner — üçü de yeni `systemLocale` alanı + `i18n.SystemLocale` varsayılanı ile, `getenv` alanıyla birebir aynı desen). `parseLocaleLikeValue`'nun BCP-47 etiketlerini (`tr-TR`, `TR-tr`, bare `tr`) doğru ayrıştırdığı varsayılmadı, test edildi. Cold-start: syscall yalnızca zincir gerçekten adıma ulaştığında (auto + hiçbir env var yok) tetiklenir — `TestResolveLanguageSystemLocaleCalledOnlyWhenReached` bunu 0/0/1 çağrı sayımıyla kanıtlıyor; `TestFAZ11ColdStartStaysWellUnderOneSecond` (500ms tavan) dokunulmadan yeşil kaldı. Testler: `internal/i18n/lang_test.go`'nun genişletilmiş `TestResolveLanguagePrecedence` tablosu (env değişkenleri sistem yerelini her zaman yeniyor; `tr-TR`/`TR-tr`/bare `tr` → TR; `en-US`/`de-DE`/boş → EN; config açık değer her şeyi yeniyor; nil `systemLocale` bugünkü davranışla birebir aynı) + `TestResolveLanguageSystemLocaleCalledOnlyWhenReached` + `internal/i18n/locale_test.go`'nun `TestSystemLocaleOnNonWindowsIsAlwaysEmpty`'si. Belgelendi: docs/CONFIGURATION.md (TR+EN "Dil çözümleme sırası"/"Language resolution order"). Gate: `go vet ./...`, `golangci-lint run` (+ `GOOS=windows golangci-lint run ./internal/i18n/...` — `gosec` G103'ün `unsafe.Pointer` kullanımını gerçekten yakaladığı, `//nolint:gosec` kaldırılıp deneyerek doğrulandı), `go test ./... -count=1`, `GOOS=windows GOARCH=amd64 go build ./...` + `GOOS=darwin GOARCH=arm64 go build ./...` (ikisi de temiz derleniyor) hepsi temiz. Gerçek Windows'ta çalışma zamanı doğrulaması bu ortamda yapılamaz — aşağıdaki "Açık" notuna bakın.
- pwsh boşluğu (yukarıda daha önce "Açık" altında bulgu olarak kaydedilmişti) kapatıldı: `comrade init powershell` artık GOOS=windows'ta kurulu HER PowerShell sürümünün kendi profiline kuruluyor — yalnızca `runtime.GOOS`'a göre tahmin edilen tek bir profile değil. `internal/shellinit`'e yeni `ResolvePowerShellProfiles(ctx, goos, lookPath, run) ([]PSProfile, error)` eklendi: GOOS=windows'ta hem `"powershell"` (Windows PowerShell 5.1) hem `"pwsh"` (PowerShell 7) `lookPath` ile aranıyor, bulunan HER birinin kendi `$PROFILE`'ı ayrıca sorgulanıyor; yalnızca HİÇBİRİ bulunamazsa (`shellinit.ErrNoPowerShellFound`) hata dönüyor — yalnızca biri bulunması hata değil. Bir varyantın ikili dosyası bulunup `$PROFILE` sorgusu başarısız olursa (nadir), o varyant `OK:false` olarak raporlanır ama diğer varyant(lar) etkilenmeden işlenmeye devam eder. non-Windows davranışı DEĞİŞMEDİ (`internal/cli/init.go`'nun dispatch'i yalnızca `shell == powershell && goos == "windows"` için yeni çoklu-profil yoluna giriyor; her yerde — non-Windows PowerShell dahil — eski tek-profilli `RCPath` yolu birebir aynı kaldı, `TestInitPowerShellFallsBackToPrintingWhenProfileCannotBeResolved` dahil tüm eski testler değişmeden geçiyor). Kurulum/kaldırma artık profil başına bağımsız çalışıyor (`ApplyBlock`/`RemoveBlock` idempotency makinesi aynen kullanılıyor), ama kurulumda TEK bir birleşik y/N onayı soruluyor (profil başına değil) ve her profil için ayrı bir çıktı satırı basılıyor (varyant adı + yol + durum). Yeni katalog anahtarları (TR+EN): `MsgInitPSVariantAlreadyInstalled`, `MsgInitPSVariantInstalled`, `MsgInitPSVariantNotInstalled`, `MsgInitPSVariantRemoved`, `MsgInitPSVariantUnresolved`, `MsgInitConfirmPromptMulti`, `MsgInitPowerShellNoneFoundError`. Varyant görünen adları (`PSVariant.Label()`: "Windows PowerShell 5.1" / "PowerShell 7") bilinçli olarak çevrilmedi — Microsoft'un kendi ürün adları, risk sınıfı/kaynak adı gibi mevcut "vocabulary, not prose" istisnası kapsamında. Testler: `internal/shellinit/psprofiles_test.go` (her iki varyant var, yalnızca biri var, hiçbiri yok → hata, bir varyant çözülüp diğeri çözülemiyor, non-Windows yalnızca pwsh soruyor) + `internal/cli/init_test.go`'ya eklenen `TestInitPowerShellWindows*` tablosu (her iki profile kurulum tek onayla, `--yes`, yalnızca bir varyant, hiçbiri yok → hata, idempotent ikinci çalıştırma, bir profil eski/stale iken diğeri sıfırdan kurulum tek onayla, `--remove` her iki profilden, `--remove` yalnızca biri kuruluyken, çözülemeyen varyant diğerini engellemiyor, TR render testleri). Bulgu (ilgisiz, o an kapsam dışıydı — SONRADAN DÜZELTİLDİ, bkz. aşağıdaki yeni "Tamamlandı" notu): `confirmYesNo` yalnızca İngilizce "y"/"yes" kabul ediyor, TR istem `[e/H]` gösterse de "e"/"evet" kabul edilmiyor; bu bağımsız, önceden var olan bir tutarsızlık. Gate: `go vet ./...`, `golangci-lint run` (+ `GOOS=windows golangci-lint run ./internal/cli/... ./internal/shellinit/...`), `go test ./... -count=1`, `go test -race ./internal/shellinit/... ./internal/cli/... -count=1`, `GOOS=windows GOARCH=amd64 go build ./...` + `go vet ./...`, `GOOS=darwin GOARCH=arm64 go build ./...` hepsi temiz. Gerçek Windows'ta yeniden kurulum + canlı doğrulama henüz yapılmadı (review+merge sonrası) — aşağıdaki "Açık" notuna bakın. Bu düzeltme daha sonra gerçek Windows makinesinde canlıya alınıp doğrulandı (commit 17ac272) — bu doğrulama sırasında AŞAĞIDAKİ yeni bulgu ortaya çıktı.
- PowerShell hook'u başarısız komutu exit 0 olarak kaydediyordu — kullanıcının gerçek canlı testinde bulundu (2026-07-10, pwsh çoklu-profil düzeltmesi canlıya alındıktan hemen sonra): pwsh 7'de `pyton --version` (yazım hatası) → "The term 'pyton' is not recognized...", ardından `comrade fix` → "the last recorded command exited successfully (nothing to fix)". Kök neden doğrulandı: `internal/shellinit/snippets/powershell.ps1`'in prompt fonksiyonu yalnızca `$global:LASTEXITCODE`'u okuyordu — bu değişken YALNIZCA native (harici) programlar için ayarlanır; bir `CommandNotFoundException` (ya da başka bir cmdlet/parse hatası) ona hiç dokunmaz, bu yüzden eski/bayat değerinde kalır (oturum başında `$null` → `0`'a eşleniyor). Doğru sinyal otomatik `$?` değişkeni — ama prompt fonksiyonunun KESİNLİKLE ilk ifadesi olarak okunmalı, çünkü ondan önce çalışan HERHANGİ bir ifade (basit bir atama bile) onu sıfırlıyor. CLAUDE.md'nin spesifikasyonu zaten "$?/$LASTEXITCODE" diyordu; eski kod bunun yalnızca yarısını kullanıyordu. Düzeltme: `$success = $?` fonksiyonun ilk satırı olarak yakalanıyor; kaydedilecek exit kodu şöyle hesaplanıyor: başarı → `0`; başarısızlık → `$LASTEXITCODE` sıfır olmayan bir sayıysa onun değeri (gerçek bir native komutun gerçek exit kodunu korur), değilse genel `1`. Bu aynı zamanda "ayna" hatasını da düzeltiyor: önceki bir native başarısızlıktan kalan bayat sıfır-olmayan `$LASTEXITCODE`, daha sonraki GERÇEKTEN başarılı bir cmdlet-only komutu da başarısız olarak kaydettirebiliyordu — artık yalnızca `$?` karar veriyor, `$LASTEXITCODE`'un bayatlığından bağımsız. Kullanıcının gerçek Windows makinesinde HEM Windows PowerShell 5.1 HEM PowerShell 7'de üç senaryo canlı doğrulandı (izole `-File` betikleriyle, doğrudan `$?`/`$LASTEXITCODE` mantığı test edilerek — `Get-History` `-File` toplu iş modunda etkileşimli oturumdaki gibi doldurulmadığından tam `Get-History`+`prompt` döngüsü senkron olarak sürülemedi, ama bu döngünün kendisi kullanıcının orijinal canlı bulgusuyla zaten kanıtlanmıştı): (1) komut bulunamadı → eski mantık `0` (hata), yeni mantık `1` (doğru) — her iki PowerShell sürümünde birebir; (2) ayna hatası (bayat `$LASTEXITCODE=5` + sonraki başarılı `Get-Date`) → eski mantık `5` (hata), yeni mantık `0` (doğru) — her iki sürümde; (3) gerçek native başarısızlık (`cmd /c exit 7`) → yeni mantık `7`'yi doğru koruyor — her iki sürümde. bash/zsh/fish kancaları da kontrol edildi: üçü de kendi tek/birleşik `$?`/`$status`'unu kancanın ilk ifadesi olarak zaten yakalıyor ve her kabuğun kendi kuralı "komut bulunamadı"yı zaten sıfır olmayan (127) olarak raporluyor — orada eşdeğer bir hata yok. Testler: `internal/shellinit/snippet_test.go`'nun `wantPowerShellSnippet` golden'ı yeni metinle güncellendi; yeni `internal/shellinit/block_test.go`'daki `TestApplyBlockUpgradesRealPreExitCodeFixPowerShellSnippet` — GERÇEK eski (düzeltme öncesi) snippet içeriğini `ApplyBlock`'a veriyor, `StatusUpgraded` döndüğünü ve sonucun yeni `Block(PowerShell)` ile birebir eşleştiğini doğruluyor (yani `comrade init powershell`'i yeniden çalıştırmak düzeltmeyi otomatik olarak devreye sokuyor). Belgelendi: docs/TECHNICAL*.md §9 kontrol edildi — mekanizmayı zaten genel düzeyde "`$?`/`$LASTEXITCODE`'u okuyan" olarak tanımlıyordu (spesifik birleşim mantığını iddia etmiyordu), bu yüzden düzeltmeyle çelişmiyor, güncelleme GEREKMEDİ. Gate: `go vet ./...`, `golangci-lint run`, `go test ./... -count=1`, `go test -race ./internal/shellinit/... ./internal/cli/... -count=1`, `GOOS=windows GOARCH=amd64 go build ./...` + `go vet ./...` hepsi temiz. Ayrıca (ilgisiz bir bulgu, ama bu değişiklik setinde düzeltildi): CHANGELOG.md'de bu girdiden hemen önceki bölümde `### Added` başlığı eksikti (`llm.idle_timeout_seconds`/dependabot girdileri yanlışlıkla `### Fixed` altında kalmıştı) — başlık geri eklendi, o iki girdi artık doğru şekilde `### Added` altında.
- `confirmYesNo` dil bulgusu düzeltildi (yukarıda 29. maddede "ilgisiz, kapsam dışı" olarak kaydedilmişti): artık `internal/cli/init.go`'nun `confirmYesNo`'su, aktif `i18n.Translator`'ın `Lang()`'ine göre yeni `isAffirmative(lang, line)` yardımcı fonksiyonu üzerinden kabul ediyor — TR: `e`/`evet` (büyük/küçük harf duyarsız) olumlu; her diğer dil (EN dahil): `y`/`yes`; her ikisi de eşleşmeyen HER ŞEY (boş satır dahil) varsayılan-HAYIR kalıyor. `internal/tui/confirm.go`'nun `mapKey`'i ile aynı disiplin (dile göre kesin ayrık kabul kümesi), ama burada `mapKey`'in tehlikeli çapraz-dil ters çevirme riski yok (TR'nin `e`'si EN'de bir negatif anlamla çakışmıyor — EN `e` zaten hiçbir şeye eşlenmiyordu). Her iki çağıran güncellendi (tek-profilli init onayı, `MsgInitConfirmPrompt` kullanan; çoklu-profilli PowerShell onayı, `MsgInitConfirmPromptMulti` kullanan) — ikisi de artık `tr.Lang()`'i `confirmYesNo`'ya geçiriyor. `TestInitPowerShellWindowsMessagesRenderInTurkish` artık gerçek TR kabul davranışını sürüyor (`"e\n"`, önceki geçici `"y\n"` işaretçisi yerine) — bu testteki eski "kapsam dışı" NOT'u kaldırıldı. Yeni tablo tabanlı testler (internal/cli/init_test.go): `TestIsAffirmative` (19 vaka: TR e/E/evet/EVET kabul, TR'nin EN y/yes'i reddetmesi, TR boş/gürültü/açık-h → hayır; EN y/Y/yes/Yes kabul, EN'nin TR e/evet'i reddetmesi, EN boş/gürültü/açık-n → hayır; bilinmeyen dil EN kuralına düşüyor) + `TestConfirmYesNoReadsLangSpecificAnswer` (confirmYesNo'nun kendisini sahte `*cobra.Command` üzerinden sürüyor, istemin stdout'a yazıldığını ve cevabın dile göre stdin'den okunduğunu kanıtlıyor). Gate: `go vet ./...`, `golangci-lint run`, `go test ./... -count=1`, `go test -race ./internal/cli/... -count=1` temiz.
- Yeniden tasarlanmış, renklendirilmiş `--help` + bekleme spinner'ı eklendi (üç alt parça — gruplar, pastel renkler, spinner — tek değişiklik setinde):
  (a) **Yardım grupları + Örnekler**: `internal/cli/root.go` artık cobra'nın `Group` mekanizmasını kullanıyor — üç grup: Core (do/fix/explain/chat), Setup (auth/init/config), Info (history/upgrade); başlıkları i18n'li (`MsgHelpGroupCore/Setup/Info`, `internal/cli/help.go`'nun `applyTranslatedHelp`'i render anında çeviriyor, Short metinlerle aynı mekanizma). `hook` (ve gizli `hook record`) ile gizli `config test-llm` hiçbir gruba atanmadı — cobra Hidden komutu GroupID'den bağımsız asla listelemiyor. Kök komuta kısa, i18n'li bir "Examples:" bölümü eklendi (`MsgHelpExamplesRoot`, `root.Example`). `comrade version` diye bir alt komut YOK (yalnızca `--version` bayrağı) — görevin "Info (history, upgrade, version)" ifadesindeki "version" bu yüzden gruba dahil edilmedi.
  (b)+(c) **Pastel renkler + doğru renk kapısı**: `internal/cli/color.go`'daki yeni `resolveColorEnabled(cfg, environ, out)` — repodaki TEK renk karar noktası — `cfg.General.Color` (ana açma/kapama) ile `colorprofile.Detect` (NO_COLOR/CLICOLOR_FORCE/gerçek TTY tespiti, zaten dolaylı bağımlılık, artık doğrudan) birleştiriyor; sonuç true ise ve `out` bir `*os.File` ise `lipgloss.EnableLegacyWindowsANSI` çağrılıyor (Windows'ta legacy conhost.exe'nin ENABLE_VIRTUAL_TERMINAL_PROCESSING'i açması için — pwsh 7/Windows Terminal zaten VT-farkında, no-op). Önceden `cfg.General.Color` HİÇBİR TTY/env farkındalığı olmadan 7 çağrı noktasında (chat.go/chatmodel.go/do.go/fix.go/explain.go/promptui.go/runtime.go) doğrudan okunuyordu — hepsi artık `resolveColorEnabled`'a yönlendirildi, İKİNCİ bir karar yolu yaratılmadı. `internal/cli/help.go`'daki yeni `colorizeHelpText` cobra/pflag'in ZATEN DOĞRU HİZALANMIŞ düz metin çıktısını satır satır tarayıp yalnızca bölüm başlıkları (lavanta/mor, kalın), komut adları (pastel camgöbeği) ve bayrak adları (pastel şeftali) etrafına ANSI kodu SARIYOR — hizalamayı YENİDEN HESAPLAMIYOR (ANSI kodları görünmez genişlikte olduğu için zaten-doğru boşluk/padding'i bozmuyor); `cmd.Name()`/flag adının KENDİSİNE renk enjekte etmek yerine bu yaklaşım seçildi çünkü cobra'nın `rpad` şablon fonksiyonu ham `len()` ile dolduruyor — ANSI kodu doğrudan `.Name`'e eklenseydi hizalamayı bozardı. `lipgloss.AdaptiveColor`/`compat` paketi BİLİNÇLİ OLARAK kullanılmadı: `charm.land/lipgloss/v2/compat`'ın `HasDarkBackground`/`Profile` paket-seviyesi değişkenleri, o paket import edilir edilmez KOŞULSUZ olarak gerçek bir terminal sorgusu (OSG 11, `lipgloss/v2/terminal.go`'da 2 saniyeye kadar bloklayan bir timeout) çalıştırıyor — bu, projenin kendi soğuk-başlangıç sertleştirme tarihçesiyle (go.mod'un atotto/clipboard yorumu, FAZ 11) doğrudan çelişen bir regresyon sınıfı olurdu; onun yerine sabit, orta-tonlu ANSI256 kodları seçildi (hem koyu hem açık terminallerde okunaklı, çalışma zamanı arka plan tespiti gerektirmeden). Yeni gate: `go mod tidy` `github.com/charmbracelet/colorprofile`'ı dolaylıdan doğrudan bağımlılığa yükseltti (versiyon değişmedi, v0.4.3 sabit kaldı).
  (d) **Spinner**: `internal/cli/spinner.go`'daki `startWaitSpinner(enabled, out, tr)` — bubbles/v2/spinner'ın kare VERİSİNİ (`spinner.MiniDot`, braille kareler) ödünç alıyor ama kendi `tea.Model`/`Update`/`View` makinesini KULLANMIYOR — dört çağrı noktasının (do/fix planlama+teşhis, explain, chat'in "/do"'su) hiçbiri o blocking LLM çağrısı sırasında aktif bir bubbletea Program içinde DEĞİL (chat'in "/do"'su zaten kendi dış Program'ının `ReleaseTerminal()`'ını çağırıyor — `chatmodel.go`), bu yüzden ikinci bir tea.Program başlatmak yerine düz goroutine+`time.Ticker` seçildi (görevin kendi "yeni bağımlılık tercih edilmez" yönlendirmesiyle de uyumlu — zaten dep olan bubbles'ın sadece veri tablosu kullanıldı). stderr'e yazıyor, i18n'li etiket (`MsgSpinnerThinking`: EN "thinking…" / TR "düşünüyorum…"), `stop()` HER ZAMAN goroutine tamamen bitene (`<-done`) kadar bloklanıp ANSI "satırı sil" (`\r\x1b[K`) dizisini yazıyor — internal/llm'in `sendChunk`/`ctx.Done()` sızıntı-önleme deseniyle aynı disiplin (sızan goroutine yok, `TestStartWaitSpinnerStopNeverLeaksAGoroutine` `runtime.NumGoroutine()` ile kanıtlıyor). `enabled` her çağrı noktasında `resolveColorEnabled(cfg, os.Environ(), cmd.ErrOrStderr())` — stdout değil stderr'e karşı çözülüyor (2(c)'nin TEK karar noktasını yeniden kullanma talimatına göre bilinçli seçim: general.color=false VEYA NO_COLOR ayarlıyken spinner TAMAMEN görünmez oluyor, yalnızca renksiz değil — bu görevin "Part 2(b)/(c) ile aynı NO_COLOR/CLICOLOR_FORCE davranışı" talimatının en doğal okunuşu). Chat'in düz metin sohbet turu (handleText → chatTurn) KASITLI OLARAK spinner'sız bırakıldı: bubbletea Program dış Update() döngüsü içinde `ReleaseTerminal()` çağrılmadan bloklanıyor, ham stderr yazımının bubbletea'nın kendi eşzamanlı render'ıyla çakışma riski var — yalnızca "/do" (zaten ReleaseTerminal çağıran) kapsama alındı; bu kasıtlı, dar bir kapsam kararı, raporda ayrıca bayraklandı.
  Yeni MessageID'ler (TR+EN): `MsgHelpGroupCore`, `MsgHelpGroupSetup`, `MsgHelpGroupInfo`, `MsgHelpExamplesRoot`, `MsgSpinnerThinking`. `internal/cli/catalog_coverage_test.go`'nun allowlist'ine yeni bir girdi eklendi: `spinner.go` — satır-silme dizisi `"\r\x1b[K"` harf-içeren ama DÜZ YAZI OLMAYAN (K, ANSI kodunun bir parçası) tek bir literal, linter'ın `containsProseLetter` sezgisi bunu ayırt edemiyor; **docs/TECHNICAL*.md §10'un "hook.go tek allowlist'lenmiş dosya" iddiası artık YANLIŞ** — docs agent'ının düzeltmesi gerekiyor. Testler: `internal/cli/init_test.go` (Part 1, yukarıda), `internal/cli/color_test.go` (`TestResolveColorEnabled` — 7 vaka: config kapalıyken her zaman kapalı, gerçek/zorlanmış TTY, NO_COLOR, CLICOLOR_FORCE piped, TTY'de NO_COLOR CLICOLOR_FORCE'u yeniyor, piped'de CLICOLOR_FORCE NO_COLOR'u yeniyor — colorprofile v0.4.3'ün DOĞRULANMIŞ gerçek davranışı, doc yorumundaki iddiadan farklı, kaynak koduyla doğrulanıp sabitlendi), `internal/cli/help_color_test.go` (`colorizeHelpText`'in tam ANSI çıktısını birebir sabitleyen testler + uçtan uca `CLICOLOR_FORCE=1` kanıtı), `internal/cli/spinner_test.go` (devre dışı=no-op, kare yaz+stop'ta temizle, TR etiket, goroutine sızıntısı yok, stop'un güvenli tek-çağrı deseni), `internal/cli/do_test.go`'ya eklenen `TestDoShowsSpinnerOnStderrWhileWaitingForPlan` (uçtan uca kablolama kanıtı: spinner stderr'de, asla stdout'ta değil). Manuel doğrulama: `CLICOLOR_FORCE=1 comrade --help | cat -v` gerçek escape kodlarını gösterdi (25 bayt), CLICOLOR_FORCE'suz aynı komut sıfır escape kodu üretti — ikisi de doğrulandı. Gate: `go vet ./...`, `make lint` (+ `GOOS=windows golangci-lint run ./internal/cli/... ./internal/shellinit/...`), `go test ./... -count=1`, `go test -race ./internal/cli/... ./internal/tui/... -count=1`, `GOOS=windows GOARCH=amd64 go build ./...` + `go vet ./...`, `GOOS=darwin GOARCH=arm64 go build ./...` hepsi temiz.

- Kullanıcının gerçek host'unda QA turu, yeni yardım/renk/spinner işine (yukarıdaki nottaki tüm alt-parçalar) geçti ama altı hata/kenar-durum buldu (D1-D6, hepsi TEK değişiklik setinde düzeltildi):
  - **D1 (MAJOR)**: `comrade explain --help` gerçek bir LLM çağrısı yapıyordu — `explain.go`'nun `DisableFlagParsing: true`'su (bayrakla başlayan bir komutu, örn. `-rf`, açıklayabilmek için gerekli) cobra'nın kendi otomatik `-h`/`--help` yakalamasını da devre dışı bırakıyordu, bu yüzden `"--help"` tek argüman olarak `runExplain`'e düz metin olarak geçiyordu (kullanıcının token'ları sessizce harcanıyordu; explain'in kendi usage'ı hiç ERİŞİLEMEZDİ). Kök neden: `Args: cobra.MinimumNArgs(1)` DisableFlagParsing ile birlikte hâlâ `args=["--help"]`'i (len=1≥1) geçerli sayıyordu. Düzeltme: `Args: cobra.ArbitraryArgs`'a geçildi, RunE kendi `-h`/`--help` (yalnızca TEK argümansa) ve boş-argüman kontrolünü yapıyor artık — ikisi de LLM'e HİÇ ulaşmadan `cmd.Help()`/i18n'li usage hatası dönüyor. Belgelenmiş kaçış yolu korundu: `comrade explain -- --help` `--help`'i harfiyen açıklıyor (LLM'e ulaşıyor) — `--` DisableFlagParsing altında cobra tarafından hiç soyulmadığından RunE kendisi soyuyor. Yeni `MsgExplainUsageError` (TR+EN). Testler: `TestExplainHelpFlagNeverCallsTheProvider` (sayaçlı mock sunucu, `--help`/`-h` ikisi de 0 istek), `TestExplainNoArgsShowsUsageErrorWithoutCallingProvider` + `...InTurkish`, `TestExplainDoubleDashEscapeHatchExplainsLiteralHelpFlag` (kaçış yolunun GERÇEKTEN sağlayıcıya ulaştığını kanıtlıyor, tam 1 istek).
  - **D2 (MAJOR)**: `comrade config set --help` "accepts 2 arg(s), received 1" hatasıyla patlıyordu (usage keşfedilemezdi; `config get --help` zaten çalışıyordu — o `DisableFlagParsing` kullanmıyor). Aynı kök neden: `newConfigSetCmd`'nin `DisableFlagParsing: true`'su (değer olarak `-5` gibi eksi işaretli sayılara izin vermek için) artı `Args: cobra.ExactArgs(2)` — `--help` tek argüman olarak ExactArgs'ı RunE'ye hiç ulaşmadan reddediyordu. Düzeltme: `Args: cobra.ArbitraryArgs`'a geçildi, RunE kendi `-h`/`--help` ve 2-argüman-değil kontrolünü yapıyor (yeni `MsgConfigSetUsageError`, TR+EN) — negatif değer geçişi (`config set llm.timeout_seconds -5`) davranışça DOKUNULMADAN kaldı. Testler: `TestConfigSetHelpFlagExitsZeroWithUsage` (`--help`/`-h`), `TestConfigSetWrongArgCountShowsTranslatedUsageError` + `...InTurkish`.
  - **D3 (MAJOR)**: `comrade upgrade --check` GitHub'ın ham İngilizce 404 JSON gövdesini stderr'e döküyordu (yayımlanmış hiçbir release olmayan bir repo için — GitHub'ın "latest release" endpoint'inin GERÇEK davranışı). Kök neden: `internal/update/github.go`'nun `LatestRelease`'i her non-200 durumda `fmt.Errorf("...unexpected status %d: %s", status, body)` ile ham gövdeyi (4096 bayta kadar) doğrudan hata metnine gömüyordu. Düzeltme: yeni sentineller `update.ErrReleaseNotFound` (404'e özel) ve `update.ErrFetchFailed` (fetch adımının HER başarısızlığını — istek kurma, ağ, non-200/non-404, decode — saran daha geniş aile; `ErrReleaseNotFound` da bunu sarıyor). `internal/cli/upgrade.go`'nun yeni `translateUpgradeFetchError` fonksiyonu `errors.Is` ile sınıflandırıyor: `ErrReleaseNotFound` → temiz i18n'li mesaj (`MsgUpgradeNoReleaseFound`); diğer `ErrFetchFailed` → kısa, genel i18n'li mesaj (`MsgUpgradeFetchFailed`, argümansız — ham detay ASLA birincil mesaja karışmıyor); ham detay yalnızca `COMRADE_DEBUG` set edilmişse `hook.go`'nun kurduğu KONVANSİYONLA aynı şekilde stderr'e yazılıyor. `Apply`'ın SONRAKİ, TAMAMEN AYRI hata sınıfları (platform için eşleşen asset yok, checksum uyuşmazlığı, ikili değiştirme başarısız) BİLİNÇLİ OLARAK dokunulmadı — zaten kendi başlarına yeterince anlamlı, D3'ün kapsamı yalnızca fetch adımıydı. Testler: `internal/update/github_test.go`'daki `TestGitHubClientLatestRelease404IsErrReleaseNotFound` + `TestGitHubClientLatestReleaseOtherNonOKStatusNeverIncludesFullRawBody` (5000 baytlık gövdenin asla tam sızmadığını kanıtlıyor); `internal/cli/upgrade_test.go`'daki `TestUpgradeCheckReleaseNotFoundRendersCleanMessage` + `...InTurkish`, `TestUpgradeCheckFetchFailedRendersCleanMessageWithoutRawDetail`, `TestUpgradeCheckFetchFailedDebugDetailOnlyWhenComradeDebugSet`, ve GERÇEK `update.GitHubClient`'ı sahte bir 404 httptest sunucusuna karşı sürüp `comrade upgrade --check`'in tam RunE'sinden geçiren `TestUpgradeCheckAgainstRealGitHubClient404NeverLeaksRawJSONBody` (D3'ün en sadık tekrarı).
  - **D4a (MAJOR, i18n — yazarı BİZ olan hatalar)**: QA `unknown config key ... valid keys are ...`'in `general.language=tr` altında bile İngilizce kaldığını buldu. Kök neden: `internal/config/validate.go`'nun `unknownKeyError`/`Validate`'i düz `fmt.Errorf` döndürüyordu — `internal/config` paketinin i18n bağımlılığı YOK (bilinçli, katman ayrımı) ve `internal/cli/config.go` bu hataları OLDUĞU GİBİ geri döndürüyordu. Düzeltme: `internal/config`'e YENİ yapılandırılmış hata tipleri eklendi — `*config.UnknownKeyError{Key, ValidKeys}` ve `*config.InvalidValueError{Key, Raw, Reason, Enum}` (`Reason` bir `InvalidValueReason` enum'u: `ReasonInvalidEnum`/`ReasonNotBoolean`/`ReasonNotInteger`/`ReasonNotPositive`/`ReasonNotNonNegative`) — her ikisinin `Error()`'ı önceki ham İngilizce metinle BİREBİR AYNI (mevcut hiçbir `ErrorContains`/exact-match testi bozulmadı), ama artık `internal/cli/config.go`'nun yeni `translateConfigError(tr, err)`'i `errors.As` ile bunları YAKALAYIP 6 yeni MessageID (`MsgConfigUnknownKey`, `MsgConfigInvalidEnum`, `MsgConfigInvalidBool`, `MsgConfigInvalidInt`, `MsgConfigNotPositive`, `MsgConfigNotNonNegative`; EN değerleri eski ham metinle birebir aynı) üzerinden YENİDEN RENDER ediyor — ham İngilizce metni PARSE ETMEDEN. `config set` doğrulama HÂLÂ config yüklenmeden ÖNCE oluyor (ilk-çalıştırma bildirimi kirlenmesin diye) — bu yüzden `envOnlyTranslator()` kullanıyor (runtime.go'daki YERLEŞİK, gerekçeli desen — COMRADE_LANG/LANG/LC_ALL/Windows yereli, config'in kendisi değil); `config get`/`Source`/`Set` ise config ZATEN yüklendiği için gerçek çözülmüş `general.language`'ı kullanıyor. Testler: `TestConfigSetUnknownKeyRejectedInTurkish`, `TestConfigSetInvalidEnumRejectedInTurkish` (tam cümle eşleşmesi), `TestConfigGetUnknownKeyRejectedInTurkish` (config dosyasında `general.language=tr` yazılı, `get` yolunun AYRI kod yolunu kanıtlıyor).
  - **D4b (i18n — cobra'nın kendi ürettiği metinler)**: cobra'nın `defaultUsageTemplate`'i sekiz yapısal bölüm etiketini ("Usage:"/"Aliases:"/"Examples:"/"Available Commands:"/"Additional Commands:"/"Flags:"/"Global Flags:"/"Additional help topics:", artı sondaki "Use \"...\" for more information..." satırı) ham İngilizce literal olarak gömüyor, hiçbir per-komut override noktası yok. `internal/cli/help.go`'nun yeni `usageTemplateFor(tr)`'si spf13/cobra v1.10.2'nin `command.go`'daki KENDİ `defaultUsageTemplate`'inin BİREBİR YAPISAL KOPYASI (aynı alanlar, aynı kontrol akışı) — yalnızca sekiz etiket `tr.T(...)` çağrılarıyla değiştirilmiş; `root.SetUsageTemplate(...)` ile TÜM ağaca (SetHelpFunc/SetUsageFunc ile aynı miras mekanizması) uygulanıyor. `MsgHelpMoreInfo` özel: sıfır-argümanlı `tr.T()` çağrısıyla çözülüyor çünkü katalog değeri cobra'nın KENDİ `{{.CommandPath}}` şablon sözdizimini HARFİYEN içeriyor (Translator.T'nin sıfır-argüman sözleşmesi: fmt.Sprintf'ten HİÇ geçmiyor) — her komutun kendi CommandPath'i render ANINDA cobra'nın şablon motoru tarafından dolduruluyor, önceden pişirilmiş bir değer DEĞİL. BİLİNÇLİ, BELGELENMİŞ sürüm-kayması riski: cobra'nın kendi `defaultUsageTemplate`'i export edilmemiş, programatik türetilecek bir yol yok — `usageTemplateFor`'un kendi doc yorumu bunu açıkça uyarıyor; `go.mod` cobra'yı TAM sürüme sabitlediğinden bu yalnızca bilinçli bir sürüm yükseltmesinde bayatlayabilir, asla build zamanında sessizce değil. Gerçek bir bekçi var (yalnızca yorum değil): `TestUsageTemplateForMatchesCobraDefaultShapeInEnglish` (help_test.go) — TEMSİLİ bir komut ağacını HEM `usageTemplateFor(EN)` HEM cobra'nın DOKUNULMAMIŞ varsayılan şablonundan geçirip BİREBİR AYNI çıktı üretmesini kanıtlıyor (EN katalog değerleri cobra'nın kendi literal metniyle karakter-karakter aynı olduğu için) — gelecekte cobra kendi şablonunu değiştirirse bu test SESSİZCE değil YÜKSEK SESLE bozulur. `colorizeHelpText`'in başlık-tanıma haritaları da GÜNCELLENDİ: artık `tr.T(...)` ile ÇÖZÜLMÜŞ etiketleri arıyor, sabit İngilizce literal DEĞİL — aksi halde TR render'ının "Kullanım:"/"Bayraklar:" başlıkları asla eşleşmeyip renksiz kalırdı (görevin kendisi bunu açıkça istedi). `completion` komutu (`root.CompletionOptions.HiddenDefaultCmd = true`) --help'ten GİZLENDİ (İşlevsellik kaybı YOK — `comrade completion bash` hâlâ çalışıyor) — kendi birkaç KB'lık iç yardım metnini çevirmenin gerçekten pratik olmadığı bir yargı çağrısı (görev bunu açıkça izin veriyor). Testler: `TestColorizeHelpTextRecognizesTranslatedStructuralHeaders`, `TestRootHelpStructuralHeadersRenderInTurkish` (kök `--help`'in ulaştığı her etiketin TR olduğunu, hiçbir İngilizce orijinalin OLMADIĞINI kanıtlıyor), `TestCompletionCommandIsHiddenFromHelpButStillFunctional`.
  - **D5 (MINOR)**: `comrade hook --help` tamamen boş bir "Usage:" satırı gösteriyordu. Kök neden: `hook` Runnable değildi (RunE yok) VE tek çocuğu `hook record` de Hidden (bilinçli, `TestHookRecordIsHiddenFromHelp` bunu sabitliyor — bu yüzden `hook record`'u AÇIĞA ÇIKARMAK ele alınıp REDDEDİLDİ) — cobra'nın şablonundaki İKİ "Usage:" dalı da (`.Runnable` VEYA `.HasAvailableSubCommands`) hiç ateşlemiyordu. Düzeltme: `newHookCmd`'ye `RunE: func(cmd, _) error { return cmd.Help() }` eklendi — YALNIZCA `.Runnable()`'ı true yapmak için (cobra'nın kendi "RunE yoksa ve alt komutlar varsa help göster" varsayılan davranışının AÇIK bir tekrarı; gerçek kabuk kancalarında ASLA tetiklenmiyor, hepsi doğrudan `comrade hook record ...` çağırıyor). `hook` kökten hâlâ tamamen gizli. Test: `TestHookHelpRendersNonEmptyUsageLine`.
  - **D6 (MINOR)**: `comrade fix` çıktısında aksi halde tamamen TR olan bir diyagnozun içinde yabancı bir İngilizce etiket vardı: `"Suggested verification: ..."`. Kök neden: LLM'den GELMİYORDU — `internal/engine/verify.go`'nun `printVerificationSuggestion`'ı ham, hiç i18n'den geçmemiş bir `fmt.Fprintf(deps.Stdout, "\nSuggested verification: %s\n", command)` idi (RunDeps zaten bir `Translator` alanı taşıyor ve `d.tr()` erişimcisi VAR, ama bu tek fonksiyon hiç kullanmıyordu). Aynı taramada, AYNI dosyada, AYNI önceden-yönlendirilmemiş desenle `runVerification`'ın İKİ kardeş dizesi de bulundu (`"verification: %s succeeded"` / `"verification: %s still fails (exit %d)"` — auto/ask modunda gerçek yeniden-çalıştırma sonrası basılıyor) ve AYNI değişiklik setinde düzeltildi. Yeni MessageID'ler: `MsgVerificationSuggestion`, `MsgVerificationSucceeded`, `MsgVerificationStillFails` (TR+EN); üçü de `deps.tr()` üzerinden çözülüyor. Testler: `TestOfferVerificationInfoModePrintsSuggestionInTurkish` (tam eşleşme), `TestOfferVerificationAutoModeReportsSuccessInTurkish`, `TestOfferVerificationAutoModeReportsFailureInTurkish`.
  Gate (tüm D1-D6 için ortak, tek geçiş): `go vet ./...`, `make lint` (0 issue — 8 revive uyarısı [açıklama biçimi + kullanılmayan test parametresi] önce düzeltildi), `go test ./... -count=1`, `go test -race ./internal/cli/... -count=1`, `GOOS=windows GOARCH=amd64 go build ./...` + `go vet ./...` hepsi temiz. `internal/i18n/catalog_test.go`'nun `TestCatalogsCoverIdenticalKeys`/`HaveNoEmptyValues`'ı ve `internal/cli/catalog_coverage_test.go`'nun her iki testi de değişmeden yeşil (bu turda allowlist'e YENİ girdi eklenmedi — tüm yeni hatalar ya yapılandırılmış tip+`errors.As` ya da doğrudan `tr.T(...)` üzerinden gitti).
  - **Kalan bulgu KAPANDI** (gerçek host'ta yeniden doğrulama: D1-D6 6/6 doğrulandı, yalnızca bu kaldı): `comrade explain` (argümansız) ve `comrade config set` (yanlış argüman sayısı) usage hataları, config dosyasında `general.language="tr"` YAZILIYKEN ve HİÇBİR dil env değişkeni (COMRADE_LANG/LANG/LC_ALL) ayarlı DEĞİLKEN hâlâ İngilizce render ediyordu (host: en-US yerel, env değişkeni yok) — her iki yol da `envOnlyTranslator()`'ı kullanıyordu (config'i bilinçli olarak HİÇ okumuyor), TR birim testlerim `COMRADE_LANG=tr` set ederek geçtiği için bu gerçek host senaryosunu YAKALAMAMIŞTI. Düzeltme: yeni `bestEffortTranslator(cmd, newLoader)` (runtime.go) — `loadConfigWithNotice`'i (gerçek `general.language`'ı config'ten okuyan AYNI mekanizma) dener, HERHANGİ bir hata durumunda (yüklenemeyen config) `envOnlyTranslator()`'a NAZİKÇE düşer, hata yolunun KENDİSİNİ asla başarısız kılmaz. `explain`'in HEM argümansız HEM `-- ` sonrası boş kalan yolu, `config set`'in yanlış-argüman-sayısı yolu buna geçirildi; `config set`'in DEĞER DOĞRULAMA yolu (2 argümanla geçersiz bir değer) BİLİNÇLİ OLARAK `envOnlyTranslator()`'da KALDI (o özel "dosya oluşturma yan etkisinden önce reddet" gerekçesi hâlâ geçerli — coordinator'ın kendi ayrımı). Yeni testler: `TestExplainNoArgsShowsUsageErrorInTurkishFromConfigLanguageAlone`, `TestExplainNoArgsShowsUsageErrorInEnglishWhenNeitherConfigNorEnvSetLanguage`, `TestConfigSetWrongArgCountShowsTranslatedUsageErrorInTurkishFromConfigLanguageAlone`, `TestConfigSetWrongArgCountShowsTranslatedUsageErrorInEnglishWhenNeitherConfigNorEnvSetLanguage` (dördü de config dosyasında `language="tr"` YAZILI + üç env değişkeni de açıkça boşaltılmış halde tam host senaryosunu sabitliyor; EN varsayılan durumu da hem doğru dili hem her komutun ilk-çalıştırmada zaten yaptığı config-dosyası-oluşturma davranışının artık BU yollarda da tutarlı kaldığını kanıtlıyor). Gate: `go vet ./...`, `make lint` (0 issue), `go test ./... -count=1`, `go test -race ./internal/cli/... -count=1` hepsi temiz.
- `comrade chat`'e pastel renklendirme eklendi (kullanıcı=gri 245, `/help`'in slash-komut token'ları=mavi 111, `>` istem sembolü=sarı 222; asistan yanıtları bilinçli olarak DOKUNULMADAN bırakıldı — markdown/backtick ayrıştırma kapsam dışı), hepsi `resolveColorEnabled` üzerinden tek karar noktasından geçiyor (`chatModel`'e yeni `colorEnabled` alanı, `runChat`'te çözülüyor). Bu arada gerçek bir ÖNCEDEN VAR OLAN sızıntı bulundu: `bubbles/v2/textinput`'un kendi varsayılan `Prompt` stili KOŞULSUZ olarak renkliydi (`Color("7")`, hiçbir NO_COLOR/TTY farkındalığı yok) — `setChatInputPromptStyle` ile kapatıldı. Beş mevcut pastel kod (183/115/216 + yeni 245/111/222) artık `color.go`'da adlandırılmış sabitler (önceden help.go/spinner.go'ya saçılmış ham literal'lerdi — davranış DEĞİŞMEDİ, aynı değerler). Testler: `internal/cli/chatmodel_color_test.go` (yeni dosya) — devre dışıyken tam eşleşme (byte-clean), etkinken tam ANSI sabitleme, asistan yanıtlarının HİÇBİR ZAMAN dokunulmadığının kanıtı, ve gerçek headless bubbletea Program üzerinden iki uçtan uca test. **İKİNCİ TUR (aynı sızıntı ikinci bir yüzeyde bulundu ve KAPATILDI):** bağımsız review, `internal/tui/confirm.go`'nun ask-modu düzenleme (`[d]üzenle`) textinput'unun (satır ~148-149, `textinput.New()` + `ti.Prompt = "> "`) AYNI koşulsuz sızıntıyı taşıdığını buldu — `confirmModel` zaten `colorEnabled` taşıyordu. Aynı desen uygulandı: yeni `internal/tui/styles.go`'daki `editPromptStyle(colorEnabled)` (pastel sarı etkinken, tamamen boş `lipgloss.Style{}` değilken — `riskBadgeStyle`/`warningStyle`/`commandStyle` ile BİREBİR AYNI mevcut desen), `newConfirmModel`'de hem `Focused` hem `Blurred` `Prompt` stiline uygulanıyor. **Sabit-tekrarı kararı**: `internal/tui`, `internal/cli`'yi import EDEMEZ (tersi zaten doğru yön — döngü olurdu) — bu yüzden `internal/tui/styles.go`'da YENİ, dışa açık (exported) bir `PromptYellow = "222"` sabiti eklendi (color.go'daki `paletteYellow`'un BİLİNÇLİ, minimal, elle-tutarlı-tutulan bir aynası, kod yorumunda açıkça işaretlendi) — paylaşılan bir renk paketi oluşturmak TEK bir sabit için orantısız bulundu. Bekçi (guard) `internal/cli` tarafında yaşıyor (zaten `internal/tui`'yi import etmesine İZİN VAR — doğru, mevcut bağımlılık yönü): yeni `TestPromptYellowMatchesTUIPackage` (`color_test.go`) `paletteYellow == tui.PromptYellow`'u doğruluyor, biri diğerinden BAĞIMSIZ değişirse anında kırılıyor — tek-yönlü değil, gerçek bir çift-yönlü senkronizasyon bekçisi (Derive-or-Guard). Yeni testler: `TestConfirmModelEditPromptColoredWhenEnabled` (222 var), `TestConfirmModelEditPromptPlainWhenDisabled` (hiç ANSI yok — `m.input.Focus()` GERÇEK `Update()`'in düzenleme-moduna girişiyle BİREBİR eşleşecek şekilde çağrıldı, aksi halde `editing=true` ama Blur'lı durumdaki AYRI bir "Text" rengi sızıntısını yanlışlıkla yakalıyordu — bu, testi yazarken canlı olarak bulunan bir metodoloji hatasıydı, düzeltildi). **Kalan, KAPANMAMIŞ bulgu**: sanal imlecin (cursor) kendi render'ı (`\x1b[7;37m`, ters-video blok) HER İKİ yüzeyde de (chat girişi VE confirm düzenleme girişi) hâlâ koşulsuz — bu turun kapsamı yalnızca `>` istem sembolüydü, imleç BİLİNÇLİ OLARAK dokunulmadı, ikisinde de kod yorumlarıyla açıkça bayraklandı. Gate: `go vet ./...`, `make lint` (0 issue), `go test ./internal/tui/... ./internal/cli/... -count=1 -race` hepsi temiz. docs/TECHNICAL*.md senkronizasyonu GEREKİYOR (düzenlemedim — §5'in `comrade chat` alt bölümüne kısa bir pastel-stil notu + HER İKİ yüzeydeki prompt-sızıntısı-kapatma notu eklenmeli, artık yalnızca chat değil; mevcut "Color is decided in exactly one place" paragrafı zaten chat'i listeliyordu ama bu YENİ somut detaylar eksik).

- **Bulgu (kullanıcı, gerçek Windows makinesi, `general.language=tr`, `llm.provider=anthropic`): `comrade chat`'e yazılan mesaj HİÇ cevap almıyordu — kapatıldı.** İki ayrı kök neden bulundu ve TEK değişiklik setinde düzeltildi (repro: gerçek Anthropic API'ye hiç dokunmadan, `internal/cli` içindeki mevcut sahte `chatLLM`/`chatDoRunner` test seamleri + Context7 üzerinden doğrulanmış Anthropic Messages API referansı ile):
  1. **(ASIL fonksiyonel hata, MAJOR)**: `chatTurn` (`internal/cli/chat.go`) düz-metin sohbet turunun `llm.CompletionRequest`'inde `MaxTokens`'ı HİÇ ayarlamıyordu — Go sıfır değeriyle (`0`) gidiyordu. `max_tokens` Messages API'de ZORUNLU bir alan (1-200000 aralığı — `charmbracelet/anthropic-sdk-go`'nun `MessageNewParams` referansı üzerinden Context7 ile doğrulandı, 2026-07); `anthropicConnector`'ın istek struct'ında `max_tokens` alanının `omitempty` etiketi YOK, bu yüzden tam olarak `"max_tokens": 0` telden gidiyordu — gerçek API bunu HER SEFERİNDE 400 ile reddediyor. Bu, `llm.provider=anthropic` olan HERKES için düz-metin sohbetin baştan beri (FAZ 9'dan, `comrade chat` hiç canlı/etkileşimli test edilmemiş olduğundan) koşulsuz kırık olduğu anlamına geliyor — 2db5b73'ün (yardım/spinner/renk seti) bir regresyonu DEĞİL. Bu paketteki HER DİĞER `Complete` çağrı noktası (`engine.Planner`/`Explainer`/`Diagnoser`, `runner.go`, `promptui.go`, `auth.go`, `config.go`) zaten `cfg.LLM.MaxTokens` (veya sabit bir değer) geçiriyordu — yalnızca chat'in düz-metin yolu unutulmuştu. Düzeltme: `chatTurn` artık `maxTokens int` parametresi alıyor; `chatController`'a yeni `maxTokens` alanı eklendi, `newChatModel` bunu `cfg.LLM.MaxTokens`'tan dolduruyor. Regresyon testi: `TestDispatchChatLinePlainTextRequestUsesConfiguredMaxTokens` (internal/cli/chatdispatch_test.go) — düzeltme öncesi kodda (`git show HEAD:...` ile üretilen orijinal üç dosyaya karşı elle doğrulandı) `fake.calls[0].MaxTokens == 0` çıktığı kanıtlandı, düzeltmeden sonra `2048` (config varsayılanı) geçiyor.
  2. **(mekanik/UX bulgu, katkıda bulunan neden)**: `chatModel.Update` (`internal/cli/chatmodel.go`) Enter'a basıldığında `chatTurn`'ü (ve "/do"'nun tüm plan+execute pipeline'ını) DOĞRUDAN, SENKRON çağırıyordu — bubbletea'nın TÜM render döngüsünü çağrının süresi boyunca (varsayılan `llm.timeout_seconds=60`'a kadar) dondurarak: hiçbir spinner, hiçbir yeniden çizim, Ctrl-C bile YOK. Bu, madde 1'in kök nedenini büyütmüyor (400 hatası hızlı döner) ama genel olarak "hiç cevap vermedi" algısını pekiştiren, kendi başına da gerçek bir kusur: 60 saniyeye kadar dondurulmuş bir terminal, kullanıcının aracın çöktüğünü/asıldığını düşünmesine yeter. Düzeltme: Enter artık yeni bir `runChatTurnCmd` üzerinden ASENKRON bir `tea.Cmd` gönderiyor (`chatTurnDoneMsg` ile geri dönüyor); bekleme süresince `charm.land/bubbles/v2/spinner` ile modelin İÇİNDE bir spinner gösteriliyor (do/fix/explain'in mevcut bekleme spinner'ıyla AYNI pastel stil/kare seti yeniden kullanılıyor — `waitSpinnerFrames`/`waitSpinnerStyle`, spinner.go), cevap/hata gelince temizleniyor; Ctrl-C artık tur beklerken de HER ZAMAN anında çıkıyor. Enter, zaten bekleyen bir tur varken (üst üste binen istekleri önlemek için) yok sayılıyor.
  Testler: `internal/cli/chatmodel_test.go` (YENİ dosya — önceden `chatModel.Update`/`View` HİÇ test edilmiyordu, yalnızca saf `chatController.dispatchChatLine` test ediliyordu): `TestChatModelEnterDispatchesTurnAsynchronouslyNotInsideUpdate` (Update'in LLM'i senkron ÇAĞIRMADIĞININ doğrudan kanıtı), `TestChatModelChatTurnDoneMsgAppendsReplyAndClearsWaiting`, `TestChatModelHeadlessProgramRoundTripsReplyIntoTranscript` + `...RendersLLMErrorInTranscript` (tam `*tea.Program`'ı uçtan uca, headless sürüyor — confirm_test.go'nun aynı tekniği), `TestChatModelEnterDispatchesDoCommandAsynchronouslyToo` ("/do"'nun da aynı async yoldan geçtiğinin kanıtı), `TestChatModelViewShowsSpinnerWhileWaitingAndHidesItOnceDone` + `...AfterLLMErrorToo` (spinner'ın görünür/kaybolma sözleşmesinin zamanlamadan bağımsız kanıtı), `TestChatModelEnterIsIgnoredWhileWaiting`, `TestChatModelCtrlCQuitsEvenWhileWaiting`. `fakeChatLLM`'e (chatdispatch_test.go) `sync.Mutex` eklendi — async yoldan `-race` altında gerçek bir veri yarışı yakalandı ve düzeltildi (`callCount()` erişimcisi). Kullanıcı görünür yeni metin YOK — spinner mevcut `MsgSpinnerThinking` (TR "düşünüyorum…"/EN "thinking…") anahtarını yeniden kullanıyor, yeni katalog girdisi eklenmedi. Gate: `go vet ./...`, `make lint` (0 issue), `go test ./... -count=1`, `go test -race ./internal/cli/... -count=1`, ek olarak `-race -count=40` ile yalnızca yeni async testler flakiness için stres testinden geçirildi, `GOOS=windows GOARCH=amd64 go build ./...` + `go vet ./...` hepsi temiz. **Bekleyen**: gerçek Anthropic key ile kullanıcının kendi Windows makinesinde canlı doğrulama — bu oturum kullanıcının API token'ını harcamamak için hiçbir gerçek sağlayıcı çağrısı yapmadı (görev açıkça yasakladı); bir sonraki gerçek `comrade chat` denemesinde hem cevabın geldiğini HEM bekleme sırasında spinner'ın göründüğünü doğrulaması gerekiyor.

- **Linux QA turu (Ubuntu-26.04, dev-e82d14c) bulguları düzeltildi — 12/12 alan davranışsal olarak zaten geçiyordu, bu tur yalnızca kalite kusurlarını hedefledi (tek değişiklik setinde):**
  - **MAJOR-1 (anahtarsız hata acemi-düşmanıydı)**: `comrade do`/`fix`/`explain`/`chat`'in HİÇBİR sağlayıcı için API anahtarı bulunamadığında verdiği hata, `internal/llm`'in ham sarmalama zincirini OLDUĞU GİBİ sızdırıyordu: `comrade do: engine: generate plan: llm: all providers failed: anthropic: no API key found for provider "anthropic"; set one of: COMRADE_ANTHROPIC_API_KEY, ANTHROPIC_API_KEY` — İngilizce (TR altında bile), iç sarmalama zinciri görünür, hiçbir onboarding işaretçisi yok. Kök neden: `internal/llm/errors.go`'da ZATEN yapılandırılmış bir `*llm.KeyMissingError{Provider, EnvVars}` vardı (`ErrAPIKeyMissing`'e `Unwrap()` ile sarılı), `Client.Complete`/`finalChainError`'ın HER katmanı `%w` ile sarıyordu — bu yüzden kaynakta YENİ bir sentinel gerekmedi, yalnızca CLI sınırında yeni bir çevirici. Düzeltme: `internal/cli/runtime.go`'ya `classifyLLMError(tr, err) (error, bool)` (saf, I/O yok — `errors.As` ile `*KeyMissingError`'ı zincirin HERHANGİ bir derinliğinden yakalıyor, yeni `MsgLLMNoKeyError`'ı render ediyor) ve `translateLLMError(w, prefix, tr, err) error` (classifyLLMError + ham detayı YALNIZCA `COMRADE_DEBUG` ayarlıyken `w`'ye döken + sınıflandırılmamış hatalar için ESKİ `"prefix: %w"` sarmalamasını AYNEN koruyan) eklendi — `translateConfigError`/`translateUpgradeFetchError`'ın KURULU errors.Is/As-sınırda-çeviri deseninin üçüncü uygulaması. `do.go`/`fix.go`/`explain.go`'nun LLM çağrı-hata kontrol noktaları `translateLLMError`'a geçirildi. Chat'in `dispatchChatLine`'ının (`chatdispatch.go`) HİÇBİR `cmd`/`io.Writer` erişimi YOK (bilinçli, belgeli tasarım) — bu yüzden yeni, saf `renderChatLLMError(tr, err) string` (yalnızca `classifyLLMError`'ı sarıyor, COMRADE_DEBUG dökümü YOK) `handleText`/`handleDo`'ya kablolandı. Tam mesaj (EN): `no API key configured for %s yet — run "comrade auth login %s" to set one up (or export its env var directly; see "comrade auth login --help")`; (TR): `%s için henüz bir API anahtarı yapılandırılmamış — kurmak için "comrade auth login %s" çalıştırın (ya da doğrudan ortam değişkenini ayarlayın; ayrıntılar için "comrade auth login --help")`. Testler: `internal/cli/runtime_test.go` (YENİ dosya) — `TestClassifyLLMErrorRecognizesRealNoKeyChain`(+TR), `TestClassifyLLMErrorLeavesOtherErrorsUnclassified` (ErrOffline/ErrAuthRejected/ErrOverloaded/ilgisiz hata SINIFLANDIRILMIYOR), `TestTranslateLLMErrorSuppressesWrapChainForNoKey`, `TestTranslateLLMErrorKeepsPrefixForUnclassifiedErrors`, `TestTranslateLLMErrorDumpsDetailOnlyWhenComradeDebugSet`, `TestTranslateLLMErrorNeverDumpsDetailForUnclassifiedErrors`; `chatdispatch_test.go`'ya eklenen `TestDispatchChatLinePlainTextNoKeyErrorIsRenderedFriendly`/`TestDispatchChatLineDoNoKeyErrorIsRenderedFriendly`. İki mevcut test güncellendi (davranış değişikliğinin doğal sonucu, regresyon DEĞİL): `TestAuthLoginStoresKeyEvenWhenPingFails` (eski "Test request failed" yerine MINOR-4-bitişik yeni "Could not verify it right now" metni), `TestRootDispatchUnmatchedArgsFallsBackToDo` (artık `"comrade do:"` ön eki KASITLI OLARAK bastırıldığından, dostane mesajın kendi içeriğine göre doğrulanıyor).
  - **MAJOR-2 (auth login kesin-reddedilmiş bir anahtarı saklıyordu)**: `internal/llm`'in `ErrAuthRejected`'ı (401/403, fallback zincirini HEMEN durduran, yeniden-denenemez sentinel) `comrade auth login`'in ping-doğrulama adımında ayırt edilmiyordu — HER ping hatası (ağ/timeout/5xx dahil) aynı "saklandı ama doğrulanamadı" yoluna gidiyordu, bu yüzden kesin olarak KÖTÜ bir anahtar bile saklı kalıyordu. İlk düzeltme store→ping→reddedilirse-sil sırasıyla yapılmıştı; **review sonrası ping-ÖNCE sıraya çevrildi** (`pingProvider` zaten girilen anahtarı DOĞRUDAN bellekteki bir `llm.WithKeyResolver` closure'ı üzerinden doğruluyor, store'a hiç dokunmuyor — bu yüzden önce yazıp reddedilirse geri almanın hiçbir gerekçesi yoktu). Son hâl: `auth.go` artık ÖNCE `pingProvider`'ı çağırıyor, SONRA `errors.Is(pingErr, llm.ErrAuthRejected)`'a göre ikiye ayrılıyor: (a) reddedildiyse → `store.Set` HİÇ ÇAĞRILMIYOR (yazma yok, silme yok, silme-başarısızlığı modu yok, bozuk anahtarın saklı kaldığı hiçbir pencere yok), yeni i18n'li `MsgAuthKeyRejected` ("%s için bu anahtar sağlayıcı tarafından reddedildi (%v) — kaydedilmedi. ...") GERÇEK bir komut hatası (sıfır olmayan çıkış kodu) olarak dönüyor; (b) diğer HER durum (ağ/timeout/5xx/parse) VEYA başarı → `store.Set` çağrılıyor (FAZ-8'in "doğrulanamasa bile sakla" davranışı ve `MsgAuthStoredKeyPingFailed`'ın yumuşatılmış metni [\"Test request failed\" → \"Could not verify it right now (%v) — this looks like a network or connectivity issue, not necessarily a bad key. The key was saved.\"] AYNEN korunuyor). Testler GÜNCELLENDİ (`TestAuthLoginRemovesKeyAndFailsWhenProviderRejectsIt*` → `TestAuthLoginNeverWritesKeyWhenProviderRejectsIt*`): artık `withUnavailableKeychain` (dosya yedeği) kullanıyor ve credentials dosyasının komuttan SONRA da HİÇ VAR OLMADIĞINI (`os.IsNotExist`) kanıtlıyor — yalnızca son durumu (`store.Get` → `SourceNone`) değil, çünkü o tek başına yaz-sonra-sil ile hiç-yazma arasında AYIRT EDEMEZDİ. Yük-taşıyıcılığı ampirik olarak doğrulandı: eski store-önce-ping sırasına GEÇİCİ OLARAK geri dönülüp test çalıştırıldı → beklenen şekilde FAIL etti (dosya OLUŞTU), ardından doğru sıraya geri alınıp PASS olduğu doğrulandı.
  - **MINOR-5 (TTY olmayan stdin ham errno gösteriyordu)**: `comrade auth login` boru hattına yönlendirilmiş/betikli bir stdin ile çalıştırıldığında `x/term.ReadPassword`'ün ham platform errno'suna ("inappropriate ioctl for device") düşüyordu. Yeni paylaşılan `isTerminalFunc` (üretimde `term.IsTerminal`, `passwordReader` ile AYNI enjeksiyon deseni) + `requireInteractiveTTY(tr, isTerminal, msgID)` (runtime.go) — config yüklendikten hemen sonra, parola isteminden ÖNCE çağrılıyor. Yeni `MsgAuthLoginRequiresTTY`. Coordinator'ın "(b) chat de aynı korumayı, kolaysa" notu da TRIVIAL bulunup UYGULANDI: `runChat` (chat.go) AYNI korumayı config yüklenmeden bile ÖNCE çalıştırıyor (bubbletea gerçek bir TTY gerektirir, aksi halde SONSUZA KADAR asılı kalır — QA'nın kendi gözlemlediği semptom) — yeni `MsgChatRequiresTTY`, `bestEffortTranslator` ile TR/EN çözülüyor. Testler: `TestAuthLoginNonInteractiveStdinReportsFriendlyError`(+TR, panic-atan bir passwordReader ile `readPassword`'ün ASLA çağrılmadığını kanıtlıyor) + yeni `internal/cli/chat_test.go`'daki `TestRunChatNonInteractiveStdinReportsFriendlyErrorInsteadOfHanging`(+TR, gerçek bir asılma yerine korumanın bubbletea'dan ÖNCE ateşlediğini kanıtlıyor — düzeltme olmasaydı bu test tüm test koşusunu zaman aşımına uğratırdı, yalnızca kendini başarısız kılmazdı).
  - **MINOR-4 (keychain dosya-yedek bildirimi sert/çevrilmemişti)**: `internal/secrets`'ın `fileFallbackWarning` sabiti hem sert ("no OS keychain available... NOT encrypted") hem HER ZAMAN İngilizceydi (o paket bilinçli olarak `internal/i18n`'e bağımlı değil). Düzeltme: `secrets.NewStore` yanına yeni `secrets.NewStoreWithWarning(path, stderr, warning string)` eklendi (`NewStore` artık onun ince bir sarmalayıcısı, kendi İngilizce varsayılanıyla — mevcut TÜM çağıranlar/testler DEĞİŞMEDEN çalışıyor); `internal/cli/newSecretsStore` artık `tr i18n.Translator` alıyor ve YENİ `MsgSecretsFileFallbackWarning`'i (EN: "cli-comrade: no system keychain found, so API keys are being saved to a local file instead (base64-encoded, not encrypted — see the file's own header for details)."; TR: "cli-comrade: sistem anahtarlığı bulunamadı, bu yüzden API anahtarları yerel bir dosyaya kaydediliyor (base64 ile kodlanmış, şifrelenmemiş — ayrıntılar için dosyanın kendi başlığına bakın).") oluşturup geçiriyor — güvenlik-ilgili gerçek (base64, şifreli DEĞİL) korunuyor, alarm tonu yumuşatıldı. `newSecretsStore`'un 6 çağırı noktası (auth.go×3, config.go, models.go, runtime.go'nun `buildLLMClient`'ı) hepsi zaten kapsamdaki `tr`'yi geçiriyor (`buildLLMClient` de artık `tr i18n.Translator` parametresi alıyor — 3 çağıranı da zaten `tr`'yi kapsamda tutuyordu). Testler: `TestAuthStatusFileFallbackWarningIsSoftenedAndTranslated`(+TR, TAM stderr metnini sabitliyor) + `internal/secrets/store_test.go`'daki `TestNewStoreWithWarningUsesCallerSuppliedText`.
  - Gate (MAJOR-1/2, MINOR-4/5 için ortak): `go vet ./...`, `make lint`, `go test ./... -count=1`, `go test -race ./internal/cli/... -count=1`, `GOOS=windows GOARCH=amd64 go build ./...` + `go vet ./...`, `GOOS=darwin GOARCH=arm64 go build ./...` — hepsi temiz (ayrıntılar için bu turun kendi ⟦RESULT⟧ raporuna bakın).

### Açık / bilgilendirici
- **docs/TECHNICAL.md + docs/TECHNICAL.tr.md, §5 (`comrade auth login`) ve §6 (LLM providers) artık YANLIŞ bir sıralama anlatıyor — düzeltilmesi GEREKİYOR (docs agent'ının işi, ben düzenlemedim).** MAJOR-2'nin ping-önce-store reorder'ından SONRA her iki dosyanın da §5'i ("The key is stored, then pinged; ... deletes what was just stored") ve §6'sı ("comrade auth login's reject-and-delete behavior ... deletes the key that was just stored") artık ESKİ (store→ping→sil) sırayı anlatıyor — anahtar artık ASLA saklanmadan ÖNCE ping'leniyor, reddedilirse hiçbir şey yazılmıyor/silinmiyor. Bu bir tek-kelimelik doğruluk sorunu DEĞİL — iki bölümde birkaç cümlelik gerçek bir yeniden-yazım gerekiyor (TR karşılıkları da aynı şekilde bayat: TECHNICAL.tr.md §5 ~satır 558-566, §6 ~satır 688-690).
- **MINOR-3 (`comrade config set`, `config.toml`'daki yorumları siliyor) — bilinçli olarak ERTELENDİ (coordinator'ın kendi açık izniyle).** Kök neden: `internal/config/loader.go`'nun `SetAndSave`'i zaten `viper.WriteConfigAs`'a dayanıyor — bu, FAZ-01'den BERİ belgeli, bilinçli bir tasarım kararı (`SetAndSave`'in kendi doc yorumu: "Comments from a hand-edited file are not preserved"), bu turda YENİ ortaya çıkan bir regresyon DEĞİL. Değerlendirilen seçenekler: (1) viper'ın kendi `WriteConfig`/`WriteConfigAs`'ı — zaten kullanılıyor, yorumları HER ZAMAN siliyor, viper'ın kendisinde bunu önleyecek bir mod yok; (2) mevcut dosya metninde HEDEFLİ (targeted) değer değişimi (regex/satır-tabanlı) — coordinator'ın kendisi bunu "TOML kenar durumları için kırılgan bir regex yazıcı olmadan sağlam, minimal bir uygulama hızlı mümkün değilse" göndermemeyi AÇIKÇA yasakladı; iç içe tablolar (`[llm.openai_compat]`), diziler, tırnak/kaçış kuralları, çok satırlı string'ler gibi TOML kenar durumlarının HEPSİNİ sağlam ve İYİ test edilmiş şekilde ele almak "hızlı" bir iş değil — kırılgan bir uygulama, sessizce config dosyasını bozan bir regresyon sınıfı açar (bu projenin kendi "asla ham metin parse etme" duruşuyla da çelişir, bkz. yukarıdaki D4a notu); (3) yorum-koruyan bir TOML kütüphanesine geçiş (örn. `pelletier/go-toml/v2`, zaten viper üzerinden dolaylı bağımlılık) — bunun gerçek, güncel yorum-koruma/hedefli-düzenleme API'si Context7 üzerinden doğrulanamadı (indekslenmiş belge yok) ve DAHA ÖNEMLİSİ viper'ın TÜM okuma/env-birleştirme/varsayılan-katmanlama zincirini (`SetAndSave`'in kendisi zaten `v.MergeConfig(defaults)` + `v.MergeInConfig()` + `v.Set` + yaz akışını kullanıyor) YENİDEN İNŞA etmeyi gerektirir — bu, "hızlı, minimal bir düzeltme" kapsamının çok ötesinde, kendi başına ayrı bir görev. Karar: coordinator'ın açık sanksiyonuna göre BELGELE-ERTELE — kod değişikliği YOK. Etki: `comrade config set <key> <value>` çalıştıktan sonra, config dosyasına elle eklenmiş `#` yorumları (varsa) kaybolur; anahtar/değerlerin KENDİSİ etkilenmez (doğru şekilde korunur/yazılır). Gelecek iş: yorum-koruyan bir TOML yazıcıya geçiş, kendi ayrı görevi olarak (kütüphane seçimi + tam MergeConfig/env-katmanlama zincirinin yeniden inşası + kapsamlı kenar-durum testleri) değerlendirilmeli.
- **Hiçbir shell hook'u (bash/zsh/fish/PowerShell) `stderr_tail`/`stdout_tail`'i doldurmuyor (kayıt, kod YOK — Linux QA turunun "ayrıca kaydet" notu, tekrar doğrulandı: bu kodun ilk yazımından beri var olan, bu turda YENİ ortaya çıkmayan bir boşluk).** CLAUDE.md'nin Shell Entegrasyonu bölümü bash/zsh için `exec 2> >(tee ...)` YERİNE "güvenli tmp dosya yaklaşımı" ile stderr/stdout yakalamayı öngörüyor; DÖRT `internal/shellinit/snippets/*.{sh,fish,ps1}` dosyasının HİÇBİRİ (yalnızca bash değil — kontrol edildi: `bash.sh`/`zsh.sh`/`fish.fish`/`powershell.ps1`'in dördü de yalnızca `--shell`/`--exit`/`--command` geçiriyor) stderr/stdout yakalamıyor; `comrade hook record`'un (`internal/cli/hook.go`) kendisi de böyle bir bayrak TANIMLAMIYOR. Sonuç: `comrade fix`, gerçek hata metnine ASLA sahip olmadan (yalnızca komut+exit code'la) teşhis yapıyor — PowerShell hook'unun bu turdan önce düzeltilen, AYRI `$?`/`$LASTEXITCODE` bulgusuyla karıştırılmamalı (o, exit code'un KENDİSİNİN yanlış olmasıydı; bu, exit code doğru olsa bile hata METNİNİN hiç yakalanmamasıdır). Kapsam: bu round'un dışında — dört hook'un tümüne (tmp-dosya yakalama + `hook record`'a yeni bayrak + `contextpkg`'a alan ekleme) dokunan çok yüzeyli bir iş kalemi, gelecek görev olarak kaydedildi, koda dokunulmadı.
- **i18n istisnaları güncellemesi** (yukarıdaki D4b'den — FAZ-09 tarzı listeye ekleme): daha önce kaydedilen istisnalara (cobra Use komut adları, hook.go gizli komut/debug satırı, promptui.go LLM prompt'u, ~40 "işlem: %w" hata sarmalama zinciri, spinner.go'nun ANSI satır-temizleme dizisi) ek olarak, bu QA turunun BİLİNÇLİ, gerekçeli kapsam-dışı bıraktığı kalemler: (1) cobra/pflag'in KENDİ ürettiği bayrak-ayrıştırma hataları ("unknown flag: --x", "unknown shorthand flag") — pflag bunlar için hiçbir yapılandırılmış hata tipi/sentinel sunmuyor, yalnızca ham İngilizce `fmt.Errorf` metni; güvenilir çevirisi ham metni regex/string-eşleştirmeyle parse etmeyi gerektirir (pflag sürüm güncellemelerinde kırılgan, bu projenin "ham metin parse etme" karşıtı duruşuyla çelişir); (2) `explain`/`config set` DIŞINDAKİ komutlarda (`auth login`, `chat`, `history`, vb.) cobra'nın kendi `ExactArgs`/`MinimumNArgs`/`NoArgs` doğrulayıcılarının ürettiği "accepts N arg(s), received M" / "unknown command %q for %q" metinleri — D1/D2'nin GERÇEK hatası (`--help`'in KENDİSİ erişilemez oluyordu) bu komutlarda YOK: `DisableFlagParsing` kullanmıyorlar, bu yüzden `--help` zaten doğru çalışıyor, yalnızca bir argüman-sayısı YAZIM HATASI ham İngilizce kalıyor — kullanıcı hâlâ `--help` ile doğru kullanımı öğrenebiliyor, bu D2-sınıfı "usage keşfedilemez" zararı değil; hepsini tek tek özel Args doğrulayıcılarıyla değiştirmek görevin kapsamının OLDUKÇA ötesine geçen büyük, spekülatif bir değişiklik olurdu; (3) `completion` komutunun kendi iç yardım metni (artık --help'ten GİZLİ, ama komutun kendisi hâlâ İngilizce) — birkaç KB'lık cobra-üretimi metin, i18n kancası yok; (4) cobra'nın kendi otomatik `help` komutunun `Short`/`Long` metni ("Help about any command...") — tek satırlık, düşük etkili, `InitDefaultHelpCmd`'nin GEÇ/tembel başlatma zamanlaması `applyTranslatedHelp`'in mevcut ağaç-yürüyüşü mekanizmasıyla güvenilir şekilde kesişip kesişmediği doğrulanmadı, bu riski almaya değecek kadar önemli değil; (5) cobra'nın otomatik `-h, --help` bayrağının KENDİ `"help for <command-name>"` açıklaması (her komutta ADI DEĞİŞEN dinamik bir metin) — `flagUsageByName` mekanizması SABİT, tek-katalog-girdili metinler için tasarlandı, komut-adına göre değişen bir metin için genişletmek bu tek bayrak açıklaması için orantısız karmaşıklık katardı.
- ~~**docs/TECHNICAL.md + docs/TECHNICAL.tr.md senkronizasyonu gerekiyor**~~ (düzeltildi): §10'daki "hook.go tek allowlist'lenmiş dosya" iddiası düzeltildi, artık `spinner.go`'yu da doğru şekilde ikinci allowlist girdisi olarak sayıyor (her iki dilde de doğrulandı: `docs/TECHNICAL.md`/`docs/TECHNICAL.tr.md`'nin ilgili paragrafları hem `hook.go` hem `spinner.go`'yu adlandırıyor). §3'ün `cli/` paket-haritası satırına da "color decision (color.go), wait spinner (spinner.go)" eklendi; §5/§2'ye yardım grupları/renkler/spinner'ı anlatan yeni içerik girdi (her iki dilde). Bu değişiklik setinin kendisi bu dosyaları düzenlemedi — docs agent'ı ayrı bir geçişte yaptı.
- golangci-lint GPL-3.0 lisanslıdır; yalnızca ayrı süreç (CI/dev aracı) olarak çağrılır, koda gömülmez/vendorlanmaz.
- Manuel doğrulama: `comrade init powershell`'in çoklu-profil (Windows PowerShell 5.1 + PowerShell 7) davranışı bugün (2026-07-10, pwsh boşluğu düzeltmesi main'e alındıktan — commit 17ac272 — hemen sonra) kullanıcının gerçek Windows makinesinde canlı doğrulandı: `comrade.exe` yeniden derlenip aynı kuruluma (`C:\Users\firat\AppData\Local\Programs\comrade\comrade.exe`) yeniden yazıldı, `comrade init powershell -y` çalıştırıldı ve tam olarak beklenen iki satır gözlemlendi — `Windows PowerShell 5.1: cli-comrade shell integration is already installed in C:\Users\firat\Documents\WindowsPowerShell\Microsoft.PowerShell_profile.ps1` (5.1 profili önceki (tek-profilli) çalıştırmadan zaten kuruluydu, snippet değişmediği için "already installed", "upgraded" değil) ve `PowerShell 7: Installed cli-comrade shell integration in C:\Users\firat\Documents\PowerShell\Microsoft.PowerShell_profile.ps1` (pwsh'nin kendi profili — önceden hiç dokunulmamıştı — ilk kez kuruldu). pwsh profili okunup marker'lar arasında TAM OLARAK beklenen bloğu içerdiği (fazlası/eksiği yok, dosya sıfırdan oluşturuldu) doğrulandı; 5.1 profili değişmeden kaldı. İdempotentlik canlı kanıtlandı: aynı komut ikinci kez çalıştırıldığında her iki satır da "already installed" oldu ve her iki profil dosyasının SHA-256 hash'i çalıştırma öncesi/sonrası birebir aynı çıktı (`b4a57c728b9d685fb702a8ec9d983c24627f6b24fcc40f069757def1eae0c06d` — ikisi de, snippet içeriği varyanttan bağımsız olduğu için). `pwsh.exe -NoProfile -Command "& '...\comrade.exe' --version"` de doğru çalıştı (`comrade version dev-17ac272`). PATH'te değişiklik gerekmedi/yapılmadı, kayıt değeri hâlâ dizini içeriyor (doğrulandı). Kullanıcı hemen ardından TAM OLARAK "etkileşimli oturumda hook çalışma zamanı" adımını denedi ve bu, gerçek bir çalışma zamanı hatası buldu: pwsh 7'de `pyton --version` (yazım hatası) sonrası `comrade fix` "the last recorded command exited successfully (nothing to fix)" dedi — hook komutu KAYDETTİ ama YANLIŞ (başarılı) exit koduyla. Kök neden bulundu ve düzeltildi (yukarıdaki yeni "Tamamlandı" notuna bakın: `$?`'nin prompt fonksiyonunun ilk ifadesi olarak yakalanmaması). Hâlâ bekleyen: (1) düzeltmenin kullanıcının makinesine yeniden kurulup (review+merge sonrası) aynı `pyton --version` → `comrade fix` senaryosunun bu kez `comrade fix`'in gerçek kök nedeni bulduğunu göstererek yeniden denenmesi; (2) genel olarak etkileşimli bir oturumda hook'un çalışma zamanı davranışının ilk kez uçtan uca doğrulanması (bu, tam olarak bunu yaparken bulunan bir hatayla zaten kısmen doğrulanmış oldu — `Get-History`/`comrade hook record` tetikleme mekanizması çalışıyor; yalnızca exit kodu yanlıştı ve şimdi düzeltildi).
- Manuel doğrulama: FAZ 5 planlayıcının gerçek LLM ile "docker kur" kabul senaryosu API key gerektirir (httptest mock ile uçtan uca doğrulandı); kullanıcı gerçek key ile bir kez denemeli.
- Karar (kullanıcı onaylı): TUI için bubbletea v2 seçildi; Go tabanı 1.23 → 1.25'e yükseltildi (FAZ 0 kararı bilinçli tersine çevrildi). CLAUDE.md/go.mod güncellendi.
- Manuel doğrulama (Windows): internal/executor Windows dalında süreç-ağacı öldürme tek-süreç (torun süreçler kalabilir); pwsh yok, çalışma zamanı testi Windows'ta yapılmalı — FAZ 11 sertleştirme kalemi.
- Manuel doğrulama (Windows): `general.language = "auto"`'nun Windows sistem yereli adımı bugün (2026-07-10) kullanıcının gerçek Windows makinesinde canlı doğrulandı — mekanizmanın kendisi, `internal/i18n`'e hiç bağımlı olmayan bağımsız bir P/Invoke çağrısıyla (`GetUserDefaultLocaleName`, `ret=6`, değer `"en-US"`) ayrıca kanıtlandı; `COMRADE_LANG`/`LANG`/`LC_ALL` ayarlı DEĞİLKEN ve `general.language` `auto` (varsayılan) iken `comrade --help` bu makinenin gerçek `en-US` kültürüne uygun şekilde İngilizce render etti — zincir gerçekten syscall adımına ulaşıyor ve doğru sonucu üretiyor. Hâlâ bekleyen: gerçekten `tr-TR` bölgesel ayarlı bir Windows makinede Türkçe render dalının gözlemlenmesi (bu host'un kültürü `en-US` olduğundan bu dal buradan doğrulanamadı).
- Manuel doğrulama: comrade fix gerçek LLM ile "pyton --version" senaryosu API key gerektirir (mock ile uçtan uca doğrulandı); kullanıcı gerçek key ile bir kez denemeli.
- Manuel doğrulama: gerçek OS keychain (macOS Keychain / Windows Credential Manager / Linux Secret Service) ve gerçek TTY no-echo parola girişi bu ortamda test edilemedi (go-keyring mock + injectable reader ile doğrulandı); kullanıcı bir platformda comrade auth login ile doğrulamalı.
- i18n istisnaları (gerekçeli, docs/phases/FAZ-09.md'de belgeli — confirm.go artık bu listede DEĞİL, yukarıdaki "Tamamlandı" notuna bakın): cobra Use komut adları, hook.go gizli komut/debug satırı, promptui.go LLM prompt'u, ~40 "işlem: %w" hata sarmalama zinciri, spinner.go'nun ANSI satır-temizleme dizisi. Genişletilmiş, D4b'den gelen tam liste (cobra/pflag'in kendi ürettiği bayrak/arg-sayısı hataları, completion/help komutlarının kendi metni, dinamik "help for X" bayrak açıklaması) için yukarıdaki "Tamamlandı" bölümündeki "i18n istisnaları güncellemesi" notuna bakın.
- FAZ 10 gerçek yayın için hazır ama henüz tag atılmadı: goreleaser snapshot tüm artefaktları üretiyor; gerçek release öncesi homebrew-tap / scoop-bucket / winget-pkgs hedef repoları ve tap PAT secret'ı oluşturulmalı.
- cosign imzalama .goreleaser.yaml'de yorumlu (etkinleştirme notu); anahtar/keyless kurulumu gerektirir.
- Manuel doğrulama: install.sh/install.ps1 ve comrade upgrade gerçek ağ yolu (GitHub Releases) canlı test edilmedi (mock/snapshot ile doğrulandı); ilk gerçek release'te doğrulanmalı.
- v0.1.0-rc1 hazır (tag atılmadı — kullanıcı kararı; tag push'u gerçek release pipeline'ını tetikler ve tap repoları/PAT gerekir). Soğuk başlangıç ~5ms, tüm gate + -race + govulncheck temiz. Bilinen kısıtlar KNOWN_LIMITATIONS.md'de.
