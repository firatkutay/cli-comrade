// Package llm will define the Provider interface and its connectors
// (anthropic, openai_compat, google, ollama) used to talk to large language
// model backends (introduced in FAZ 2). It is an empty placeholder in FAZ 0.
package llm
