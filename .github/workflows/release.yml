name: Release

# Runs goreleaser against a pushed "vX.Y.Z" tag — the second half of the
# supply-chain-pinning "release two-gate rule": the tag push (git side)
# and this workflow's artifact publish are independent gates, both
# required before a release is considered done.
on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  # Grants this job's job-level GITHUB_TOKEN the ability to mint a
  # short-lived OIDC ID token. npm CLI >=11.5.1 (see the setup-node step
  # below) detects this automatically when publishing from GitHub
  # Actions and exchanges the OIDC token for a one-off, per-publish npm
  # registry credential via npm's Trusted Publishing flow -- there is no
  # NPM_TOKEN/NODE_AUTH_TOKEN anywhere in this workflow, on purpose (see
  # the "Publish npm packages" step). Requires each of the 6 packages to
  # have a "Trusted Publisher" configured on npmjs.com pointing at this
  # repo + this workflow filename (docs/PACKAGING.md's npm section has
  # the exact click-path) -- npm silently refuses the publish with a 404
  # if that per-package configuration is missing or mismatched.
  id-token: write
  # Required by the "Attach source SBOM to release" step below: it calls
  # actions/list-artifacts under the hood (@actions/artifact's
  # listArtifacts()) to find the SBOM captured earlier in this same run,
  # before uploading it as a release asset. Documented at
  # https://github.com/anchore/sbom-action#permissions ("If attaching
  # release assets, the actions: read permission is also required").
  actions: read

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # goreleaser refuses to run against a shallow clone (it walks
          # tag history for the changelog pipe even though ours is
          # disabled, and to resolve {{.Tag}}/{{.PreviousTag}}).
          fetch-depth: 0

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      # Installs the cosign CLI that .goreleaser.yaml's `signs:` block
      # invokes to key-based sign checksums.txt. Action pinned to a full
      # commit SHA (sigstore/cosign-installer v4.1.2, released 2026-05-07 —
      # matches this repo's existing convention of SHA-pinning every
      # `uses:` step, see actions/checkout and actions/setup-go above).
      #
      # cosign-release is pinned to v2.6.3, NOT the newer v2.6.4: v2.6.4
      # was published 2026-07-17, six days before this change (younger
      # than the house ≥15-day release-age floor), and its changelog is an
      # OCI-manifest/attestation-download backport with no security
      # content for the sign-blob path this repo uses. v2.6.3 (published
      # 2026-04-06, well past 15 days) additionally resolves
      # GHSA-w6c6-c85g-mmv6 relative to earlier 2.6.x releases, so it is
      # both the aging-eligible and the more conservative choice — adopt
      # v2.6.4 later once it has cleared the 15-day floor.
      - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: "v2.6.3"

      # Source SBOM, attached to the release alongside goreleaser's own
      # archives/checksums — this repo ships no container image, so
      # there is no separate "container SBOM" to also generate (see
      # security-scanning's SBOM-bundle rule; a source-only SBOM is the
      # right and complete scope for a binary-only Go CLI release).
      #
      # output-file MUST resolve outside the checked-out repo — writing
      # it in-workspace (as a bare "sbom-source.cdx.json" relative path
      # did previously) leaves an untracked file that fails goreleaser's
      # `git status --porcelain` cleanliness check ("git is in a dirty
      # state") before the release even starts (observed on the v0.1.0
      # tag's first run: https://github.com/firatkutay/cli-comrade/actions/runs/29113017395).
      # ${{ runner.temp }} is the GitHub Actions-provided scratch
      # directory, a sibling of (not nested under) github.workspace, so
      # this file is invisible to git entirely — belt-and-suspenders
      # with the /sbom-*.cdx.json .gitignore entry, which only matters
      # if a future edit points output-file back inside the workspace.
      #
      # upload-release-assets is deliberately false HERE, not because the
      # SBOM shouldn't reach the release, but because of ORDERING: this
      # step runs before goreleaser creates the release below, and the
      # release doesn't exist yet at this point in the job. Verified
      # against the action's actual source (anchore/sbom-action v0.24.0,
      # src/github/SyftGithubAction.ts's attachReleaseAssets(), read via
      # `gh api repos/anchore/sbom-action/contents/...?ref=v0.24.0`): for
      # a tag-push event (this workflow's trigger) it resolves the release
      # via `client.findRelease({tag})` -> `repos.getReleaseByTag`, which
      # 404s (silently, only a debug log, no error/failure) when no
      # release exists yet for that tag — so setting this true here would
      # just no-op, not attach anything. upload-artifact is true instead,
      # so this run's SBOM is captured as a workflow artifact that the
      # publish-sbom step below (after the release exists) can find and
      # attach — this two-phase generate-then-publish split is the
      # pattern the action's own README documents under "Publishing SBOMs
      # with releases" for exactly this "SBOM produced outside/before the
      # release step" case.
      - uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          path: .
          format: cyclonedx-json
          output-file: ${{ runner.temp }}/sbom-source.cdx.json
          artifact-name: sbom-source.cdx.json
          upload-release-assets: false
          upload-artifact: true

      # Derives the GitHub Release body from CHANGELOG.md's own
      # `## [<version>] - <date>` section (Derive-or-Guard: CHANGELOG.md
      # is the single source of truth for release notes; this step reads
      # it rather than hand-maintaining a second copy in this workflow or
      # relying on goreleaser's own commit-log changelog generator, which
      # is deliberately disabled in .goreleaser.yaml's `changelog:` block).
      # Fails the job loudly (rather than silently shipping an empty
      # release body) if the tagged version has no matching CHANGELOG
      # section — that means CHANGELOG.md wasn't cut before tagging.
      - name: Extract release notes from CHANGELOG.md
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME#v}"
          awk -v ver="$version" '
            BEGIN { pat = "^## \\[" ver "\\]" }
            $0 ~ pat { found=1; next }
            found && /^## \[/ { exit }
            found { print }
          ' CHANGELOG.md > /tmp/release-notes.md
          if [ ! -s /tmp/release-notes.md ]; then
            echo "::error::CHANGELOG.md has no '## [$version] - ...' section — cut the changelog before tagging." >&2
            exit 1
          fi

      - uses: goreleaser/goreleaser-action@f06c13b6b1a9625abc9e6e439d9c05a8f2190e94 # v7.2.3
        with:
          distribution: goreleaser
          # Exact pin, not a "~> v2.16" range: the v0.1.1 incident was
          # diagnosed by downloading v2.16.0's actual source and reading
          # internal/client/client.go + internal/tmpl/tmpl.go's
          # ApplySingleEnvOnly (see .goreleaser.yaml's repository.token
          # comment) — that diagnosis is only guaranteed to hold for
          # this exact build. A floating range could silently pick up a
          # later 2.16.x patch whose template internals differ from what
          # was verified here.
          version: "2.16.0"
          args: release --clean --release-notes=/tmp/release-notes.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # homebrew_casks/scoops/winget in .goreleaser.yaml are always
          # active and each self-gates via its own templated
          # `skip_upload: "{{ not (isEnvSet \"...\") }}"` — a channel
          # degrades to "build the manifest locally, don't push it"
          # instead of failing the whole release when its PAT is absent
          # (see the long comment in .goreleaser.yaml for the exact
          # mechanism and why it's `skip_upload`, not `disable`).
          #
          # These three MUST always be set here, even to an empty string
          # (which is exactly what `${{ secrets.X }}` renders to when the
          # named secret doesn't exist on this repo — referencing an
          # undefined secret never fails a workflow). Leaving one of
          # these lines out entirely — rather than letting it resolve to
          # "" — would make the env var wholly ABSENT from the process
          # environment instead of present-but-empty, which matters:
          # goreleaser's template engine runs Go's text/template with
          # `Option("missingkey=error")`, so a raw `.Env.VAR` reference
          # to a truly-unset key would error out — except that in
          # practice it never gets the chance to: `.goreleaser.yaml`'s
          # `repository.token:` field is only ever templated once
          # `skip_upload` has already resolved to "false", which itself
          # requires the var to be present AND non-empty (see
          # .goreleaser.yaml for the exact doPublish() ordering this
          # relies on). Keep setting all three unconditionally anyway —
          # it's what CI actually exercises and keeps that ordering
          # argument from being the only thing standing between a typo
          # here and a broken release.
          #
          # Add each PAT once you've completed the matching step in
          # docs/PACKAGING.md:
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          SCOOP_BUCKET_TOKEN: ${{ secrets.SCOOP_BUCKET_TOKEN }}
          WINGET_TOKEN: ${{ secrets.WINGET_TOKEN }}
          # Consumed by .goreleaser.yaml's `signs:` block
          # (--key=env://COSIGN_PRIVATE_KEY) to key-based sign
          # checksums.txt offline (--tlog-upload=false, no Rekor). Both
          # secrets are required for the signs pipe to succeed; unlike the
          # three packaging PATs above, signing has no skip_upload-style
          # degrade-gracefully path, so a missing secret here fails the
          # release rather than silently skipping the .sig asset.
          COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
          COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}

      # Second half of the two-phase SBOM flow started above: goreleaser
      # has now created the GitHub Release (with the archives,
      # checksums.txt and checksums.txt.sig as assets), so a release for
      # this tag exists for the first time in this job. This step uses
      # the action's dedicated `publish-sbom` sub-action (same
      # anchore/sbom-action repo/commit, `publish-sbom/action.yml`, per
      # its README's "Publishing SBOMs with releases" section) to attach
      # the SBOM workflow artifact captured by the earlier step —
      # matched by name via sbom-artifact-match, since the default
      # pattern (`.*\.spdx\.json$`) doesn't match our CycloneDX filename.
      # It does not regenerate the SBOM or touch the checked-out
      # workspace, so it carries none of the git-cleanliness risk the
      # comment above the generate step warns about.
      - name: Attach source SBOM to the GitHub Release
        uses: anchore/sbom-action/publish-sbom@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          sbom-artifact-match: "^sbom-source\\.cdx\\.json$"

      # npm publishing runs in THIS SAME job, as the steps below,
      # deliberately -- not a separate downstream job. The goreleaser
      # step above already leaves the built dist/ directory sitting on
      # this runner's filesystem; assembling the npm packages from it
      # right here means the published binaries are the literal bytes
      # goreleaser just built and cosign-signed, with zero risk of a
      # second job rebuilding (and thus diverging from) them. A separate
      # job would need dist/ re-materialized via upload-artifact +
      # download-artifact, which only adds an upload/download round-trip
      # and a second place for a version/commit mismatch to creep in --
      # no benefit for a job that already finishes in a few minutes.
      #
      # Deliberately ordered LAST, after both goreleaser and the SBOM
      # attach step above: npm publishes are irreversible (npm package
      # versions are immutable, unlike a git tag or a GitHub Release
      # asset), so every reversible, in-repo step finishes first. If npm
      # publishing fails here, the GitHub release is still complete,
      # signed, and has its SBOM attached; the reverse ordering would
      # leave npm packages live against a half-finished release. Runs
      # unconditionally after both prior steps succeed (default GitHub
      # Actions behavior: a step is skipped if any earlier step in the
      # job failed) -- if goreleaser's release didn't go out, the npm
      # packages must not go out either.
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          # npm Trusted Publishing (OIDC) requires npm CLI >=11.5.1
          # (docs.npmjs.com/trusted-publishers/, verified 2026-07-26).
          # node-version pins Node 24.18.0 (Active LTS "Krypton" as of
          # this writing, released 2026-06-23 -- past the house 15-day
          # floor) purely to get a modern npm bundled; the exact npm
          # version is still pinned explicitly below rather than trusted
          # to whatever ships with this Node build.
          #
          # npm 12.0.0/12.0.1 (released 2026-07-08/07-10) technically
          # also clear the 15-day floor as of today, but are deliberately
          # NOT adopted here: npm 12 raises its own Node engines floor to
          # `^22.22.2 || ^24.15.0 || >=26.0.0` and turns unknown/abbreviated
          # CLI flags from warnings into hard errors -- a first-major-version
          # jump with only two patch releases behind it, on a release path
          # with zero rollback margin (npm versions are immutable), is a
          # worse trade than staying on the mature, still-maintained 11.x
          # line, which already satisfies the >=11.5.1 requirement. Revisit
          # once 12.x has more patch releases behind it.
          node-version: "24.18.0"
          registry-url: "https://registry.npmjs.org"
          # Never cache in a release build -- see npm's own trusted
          # publishing example workflow (docs.npmjs.com/trusted-publishers/).
          package-manager-cache: false

      - name: Pin npm to a Trusted-Publishing-capable version
        run: npm install -g npm@11.18.0 # newest 11.x release clearing the 15-day floor as of 2026-07-26 (published 2026-06-29); see the setup-node step above for why not 12.x

      # Assembles the same 6 packages `make npm-dry-run`/`make npm-smoke`
      # exercise locally (scripts/build-npm-packages.sh), from the dist/
      # this job's own goreleaser step just produced -- never a
      # separately rebuilt dist/. Version comes from the pushed tag,
      # never hardcoded; build-npm-packages.sh independently re-verifies
      # it against dist/metadata.json's own recorded tag and refuses on
      # any mismatch (see that script's version-guard section).
      - name: Assemble npm packages from this release's dist/
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME#v}"
          make npm-package NPM_VERSION="${version}"

      # Publishes the 5 platform packages first, then the cli-comrade
      # dispatcher last -- so that by the time cli-comrade (and its
      # optionalDependencies pin) is resolvable, every platform package
      # it can depend on already exists at that exact version.
      #
      # No `npm publish --access public` flag anywhere: the 5 scoped
      # platform packages each carry `publishConfig.access: "public"` in
      # npm/platform-template/package.json already (npm reads it from
      # there); `cli-comrade` is unscoped and public by default. No
      # `--provenance` flag either -- npm's Trusted Publishing flow
      # generates and publishes provenance attestations automatically
      # once id-token: write + a matching npmjs.com Trusted Publisher
      # config are both in place (docs.npmjs.com/trusted-publishers/);
      # passing the flag explicitly would be redundant, not wrong, but
      # the automatic path is what npm's own docs demonstrate.
      #
      # Idempotent on re-run of an already-released tag: `npm view
      # <name>@<version>` is checked before every publish, and an
      # already-published version is skipped with a notice rather than
      # failing the job. A real publish failure (auth, network, registry
      # error) still fails the job loudly -- only the "this exact version
      # already exists" case is treated as a no-op. This deliberately
      # does NOT fall back to string-matching npm publish's 403 error
      # text, which is fragile and undocumented as a stable contract;
      # checking existence up front is the same check npm itself would
      # make, just visible to this workflow's control flow.
      - name: Publish npm packages via Trusted Publishing (OIDC)
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME#v}"
          pkg_dirs=(
            npm/packages/comrade-linux-x64
            npm/packages/comrade-linux-arm64
            npm/packages/comrade-darwin-x64
            npm/packages/comrade-darwin-arm64
            npm/packages/comrade-win32-x64
            npm/packages/cli-comrade
          )
          for dir in "${pkg_dirs[@]}"; do
            name="$(node -p "require('./${dir}/package.json').name")"
            echo "::group::${name}@${version}"
            if npm view "${name}@${version}" version >/dev/null 2>&1; then
              echo "npm: ${name}@${version} is already published -- skipping (safe no-op on a re-run of an already-released tag)."
            else
              npm publish "${dir}"
              echo "npm: published ${name}@${version}"
            fi
            echo "::endgroup::"
          done
