// Package redact will mask secrets (API keys, tokens, passwords) from any
// payload before it is sent to an LLM provider, acting as a mandatory
// middleware in front of internal/llm (introduced in FAZ 3). It is an empty
// placeholder in FAZ 0.
package redact
