// Package context will collect the operating environment context (OS,
// shell, working directory, last failed command, package manager) sent to
// the LLM as grounding for its plans (introduced in FAZ 3). It is an empty
// placeholder in FAZ 0.
package context
